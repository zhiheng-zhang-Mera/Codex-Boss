{
  "requirement": "R-302",
  "phase": "C",
  "date": "2026-09-09",
  "status": "PASS",
  "summary": "device self-inspection / NodeCapabilityRegistry — observed-facts-only capability states, per-node isolation",
  "implementation": [
    "src/shared/node-capabilities.ts (pure): NodeProbeData (OS/arch/CPU/GPU/RAM/runtime/browser/shell/web-logged-in/network/load), capabilityVerdicts (web-ai READY only when a provider is OBSERVED logged in), nodeStateFor (compute FAIL ⇒ FAILED; DEGRADED on partial; else READY)",
    "electron/node/node-capability-registry.ts: durable per-node records, per-node isolation, fail-closed restore",
    "electron/node/node-inspector.ts: inspectDevice gathers real host facts with overrides for determinism",
    "electron/main.ts boss:node-status IPC: inspects the desktop node from store account login state and refreshes the registry"
  ],
  "tests": [
    "tests/unit/node-capabilities.test.ts (+4): no-fake-READY (empty logged-in ⇒ DEGRADED), FAILED/DEGRADED/READY state rules, durable + isolated + corrupt fail-closed registry, real inspector probe shape"
  ],
  "gates": { "typecheck": "PASS", "vitest_unit": "57 files / 283 tests PASS", "full_build": "PASS" },
  "notes": "R-303 scheduler capability-aware routing consumes this registry next."
}
