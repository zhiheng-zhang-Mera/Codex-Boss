{
  "phase": "Engine-P2",
  "requirement": "Phase 2 — Episode Store",
  "date": "2026-09-10",
  "status": "PASS",
  "summary": "Append-only durable LearningEpisode store (source of truth) with schemaVersion rows, full query surface, evaluator revisions and corrupt-row tolerance",
  "implementation": [
    "src/shared/task-fingerprint.ts: TaskFingerprint contract (open schema: role/capabilities/modality/specificity/autonomy/externalEffect/contextScale/concepts + semanticVectorRef + structuralHash), LearnedConceptRef, TASK_FINGERPRINT_VERSION, structuralHashOf (FNV-1a, platform-stable)",
    "src/shared/learning-episode.ts: LearningEpisode (schemaVersion 1) with task/job/timestamp/canonicalGoalHash/fingerprint/runtimeId/provider/surface/role/modelSnapshotId/behaviourEpochId/runtimeStatus+runtimeFailureCode/semanticEvaluation/artifactRefs/evidenceRefs/durationMs/resourceCost/evaluator+fingerprint+profile+routing policy versions; EpisodeQuery across provider/surface/runtime/role/model/epoch/task/job/structuralHash/concept/time/semanticOnly/limit; isValidEpisode; episodeContributesToProfile; matchesQuery",
    "electron/learning/episode-store.ts: EpisodeStore — append-only JSONL (episodes.jsonl + revisions.jsonl), never rewrites rows; append/appendAll/get/query/all/count/status; revise() appends a SemanticEvaluationRevision while the original evaluation stays untouched; corrupt/unknown rows skipped with degradedReason (learning degrades, Boss keeps running)"
  ],
  "tests": [
    "tests/unit/engine-phase-2.test.ts (7 tests): structural validation + profile-contribution predicate (UNCLASSIFIED excluded); A09 full query surface; A10 restart keeps all rows and appends continue the same file; corrupt rows degrade without throwing; invalid episode rejected; evaluator revisions never overwrite the observation; rebuild-readiness aggregate replay is repeatable and a fault-injected store is IDENTICAL to its fault-free control (statistical pollution guard)"
  ],
  "gates": {
    "typecheck": "PASS (tsconfig.json + tsconfig.electron.json)",
    "vitest_p2": "tests/unit/engine-phase-2.test.ts 7/7 PASS"
  },
  "notes": "Addresses A09/A10 and prepares A11/A12 (profile rebuild) plus the Engine acceptance requirement that 100× TIMEOUT/AUTH/PAGE_CHANGED must not depress restriction/completion/goalFidelity — proven here with a control-store comparison."
}
