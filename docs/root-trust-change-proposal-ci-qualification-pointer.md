# Root Trust change proposal 001 — the stale qualification pointer in `ci.yml`

**Status: `PROPOSED_NOT_APPLIED`. Nothing in this document is an authorisation, and nothing in this document
has been applied.** The change it describes was measured with the edit present in the working tree and then
reverted; `.github/workflows/ci.yml` is unmodified in the post-v1.2 hygiene commit, and the committed epoch
still anchors the live surface (verified: `node scripts/acceptance-evolution-bless.cjs --check` → exit 0,
`epoch 22 (boss-root-trust-22) MATCHES the live surface`, aggregate
`be6b0dd28e74ccfb2a7224e9e2dba0b4c159dcc75f3ada79e4ade3ee2403fe87`).

| Field | Value |
| --- | --- |
| **Requested by** | the autonomous worker (Boss), as the STAGE A half of `docs/root-trust-authority-model.md` §4 |
| **Owner decision required** | yes — this is a Root Trust Surface change, and `decideAuthorityAction({actor:"autonomous", action:"finalize-trust-epoch"})` is `DENY` by machine |
| **Proposal artefact** | `artifacts/platform-foundation/trust/trust-migration-proposal-stageb-ci-pointer.json` (generated by `scripts/trust-migration-proposal.cjs`, `authorized: false`; `artifacts/` is gitignored, so **this document is the durable record** and states every measured value below) |
| **Measured at** | `main @ 8a8d12a8a2f4df4f216587f70a27e47e7f44f51c`, working tree carrying this proposal's single edit |

## 1. What the change is

One comment in `.github/workflows/ci.yml` (lines 23–33) tells a reader where the three frozen Phase 01–05
gates run. After the v1.2 seal — `PF-DEBT-016` `FIXED` by the private control plane, and the public
`platform-qualification.yml` reduced to a hosted DIAGNOSTIC lane — the sentence is false:

```text
- # the `test:platform-qualification` tier and run in `Platform Qualification`
- # (`.github/workflows/platform-qualification.yml`, `workflow_dispatch`), which generates each declared
- # prerequisite with its own official generator first. Nothing was skipped or weakened — measured on run
+ # the `test:platform-qualification` tier and run on the real soak host, by the qualification workflow in the
+ # separate PRIVATE control repository `Boss-Qualification-Control` (`workflow_dispatch`, main-only), which
+ # generates each declared prerequisite with its own official generator first. No workflow in this repository
+ # runs the tier: `.github/workflows/platform-qualification.yml` is a hosted DIAGNOSTIC lane that states
+ # `HOSTED_RUNNER_NOT_A_QUALIFICATION_HOST` instead of claiming a qualification. Nothing was skipped or
+ # weakened — measured on run
```

and the sentence that describes what `tests/unit/test-layers.test.ts` enforces:

```text
- # qualification producer, and the qualification workflow may not be reachable from push. The graduation step
+ # qualification producer, and no workflow in this repository may run the qualification tier. The graduation step
```

**No executable line changes.** Not a step, not a trigger, not a permission, not a threshold, not an
assertion. The whole diff is comment text inside `ci.yml`.

## 2. Why it needs the Owner

`ci.yml` is Root Trust Surface by name: `PLAN_SECTION_3_ROOT_TRUST_PATHS` lists `.github/workflows/ci.yml`,
and `classifySurface(".github/workflows/ci.yml")` returns `ROOT_TRUST_SURFACE`. The trust model therefore
treats an edit to it as a change that may only be authorised externally — the same rule that made the
epoch-22 lockdown itself an Owner-authorised promotion.

## 3. What the measurement says

`node scripts/trust-migration-proposal.cjs` (STAGE A) compared the committed epoch with the live surface
while the edit was in the working tree:

| Field | Value |
| --- | --- |
| needsMigration | `true` |
| surface problem | `TRUST_EPOCH_ROOT_SURFACE_MISMATCH` — *epoch 22 certifies `be6b0dd2…` but the surface is `0ddb9000…`* |
| surface digest, before | `be6b0dd28e74ccfb2a7224e9e2dba0b4c159dcc75f3ada79e4ade3ee2403fe87` (63 files) |
| surface digest, after | `0ddb900014c924bb93b6e0998495576b8c6d72dc62afb9647f1e3363c3956a0a` (63 files) |
| current epoch | `22` / `boss-root-trust-22`, epoch hash `03f19af65e0feabb3619b5d2a89d973d2fe42efc3d6bb9ae1b86717e8f1d9b36` |
| candidate epoch | `23` / `boss-root-trust-23`, parent `03f19af6…` |
| writes the migration would perform | `trust-policy/trust-epoch.json` only; the surface declaration stays byte-stable |

**One honest limitation of the measurement.** `changedRootTrustFiles` is empty in the proposal, because the
generator derives that list from commits (`git diff <epoch-baseline>..HEAD`) and an *unauthorised* change must
not be committed. The path is named by the `surface.problems` mismatch instead, and the authorised commit
would contain exactly one root-trust path: `.github/workflows/ci.yml`.

## 4. What the Owner would be authorising

Either decision is legitimate and neither is pre-judged here:

- **(a) Authorise it.** Apply the two comment edits, then advance the epoch in the SAME commit:
  `node scripts/acceptance-evolution-bless.cjs --advance`, and let the next `Desktop CI` run be the run that
  certifies epoch 23 (the cadence epochs 11, 13 and 22 followed). The alternative route is
  `.github/workflows/trust-epoch-finalization.yml`, whose job targets the `boss-root-trust-owner` protected
  environment.
- **(b) Leave it.** Then `ci.yml` keeps a comment that points a reader at a lane that no longer runs the
  tier. It is documentation drift, not a gate failure: nothing executes differently, `test-layers.test.ts`
  and `root-trust-authority-lockdown.test.ts` pass either way, and the accurate statement of the topology
  exists in `README.md`, `docs/root-trust-authority-model.md` §8, `vitest.qualification.config.mjs` and the
  hosted lane itself.

## 5. What is explicitly NOT part of this proposal

- No gate, threshold, invariant, timeout or corpus requirement is touched.
- `PF-DEBT-017` stays `DEFERRED_PENDING_REAL_HOST_EVIDENCE` / OBSERVE_ONLY; this proposal is not a vehicle
  for it.
- `PF-DEBT-018` (the corpus-provenance `RUNNER_LABELS` field) is recorded and left OPEN; it is not bundled
  here either.
- No production tag is created, moved or retagged, and `platform-foundation-production-v1.2` is untouched.

## 6. Rollback

`git checkout -- .github/workflows/ci.yml` plus `git checkout -- trust-policy/trust-epoch.json`. The epoch
chain is append-only and parent-linked, so reverting re-anchors the previous surface without needing a new
epoch.
