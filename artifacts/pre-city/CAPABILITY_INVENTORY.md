# Capability Inventory — Codex-Boss (pre-refactor survey)

Commit inspected: `baf4108fe16813533d9879700acc7052f56652ea` (branch `integration/pre-city-baseline`).

## How this was derived

Ground-truth sources read directly (not regenerated):

| Source | What it gave |
|---|---|
| `config/capability-modules.json` | The authoritative implementation-surface ownership map: **27 capabilities, 594 owned source files**, plus 2 `exempt` entries. |
| `config/capabilities/*.yaml` (27 files) | The declared manifests: `id`, `version`, `kind` (kernel/feature), `provides`, `requires`, `optional`, `state[]`, `health.critical`, `modules`, `bootModules`, `surface`, `permissions`. |
| `config/test-catalogue.json` | 274 suites (239 `unit`, 35 `acceptance`), each with `covers[]`, `tier`, `alwaysRun`, and most with a stated `obligation`. Zero uncatalogued test files on disk. |
| `config/architecture-baseline.json` | Ratchet baseline: `bootModuleCount 25, capabilityCount 27, dependencyEdgeCount 3, requiredEdgeCount 3, featureCapabilityCount 23, durableNamespaceCount 32`. |
| `artifacts/pre-city/raw/architecture-graph.json` | Declared graph: 27 capabilities, 3 edges, 0 cycles, bootable; kernel = `persistence, providers, runtime, state-core`. |
| `artifacts/pre-city/raw/architecture-ownership.json` | 32 durable namespaces → single owner, 0 conflicts. |
| `trust-policy/trust-epoch.json`, `root-trust-surface.json`, `requirement-retirements.json` | `trust_epoch 24` / `boss-root-trust-24`; root surface = 22 declared paths; `requirement-retirements.json` is `[]`. |
| `package.json`, `.github/workflows/{ci,platform-qualification,trust-epoch-finalization}.yml`, `.github/CODEOWNERS`, `vitest*.mjs`, `vitest.tiers.mjs` | Real acceptance-gate script names, CI job ids, test tiers. |
| `scripts/architecture.cjs`, `scripts/extend-capability-modules.cjs`, `scripts/generate-capability-manifests.cjs` | How the map and graph are derived and validated. |
| `README.md`, `STRUCTURE.md`, `docs/**` | Per-capability design docs (`docs/runtime-intelligence-plane.md`, `docs/self-cognition.md`, `docs/self-diagnosis.md`, `docs/self-case-record.md`, `docs/github-machine-identity.md`, `docs/autonomous-evolution.md`, `docs/engine-adaptive-intelligence.md`, `docs/root-trust-authority-model.md`, …). |

Two derived measurements were computed read-only from the tree itself (no artifacts written):

1. **Real lateral import graph.** Resolving every relative import inside the 594 owned files yields **187 cross-capability import edge-kinds** — vastly more than the **3 declared dependency edges**. The declared graph is a *boot-contract* graph (`config/architecture.cjs#collectImports` only inspects each manifest's `modules`, which in this repository lists only each capability's boot module, 25 files total); the real file-level graph is not represented by it.
2. **Ownership-map integrity check.** `capability-modules.json` contains three defects (see *Gaps and uncertainties*): a path that does not exist, a duplicated path within one capability, and one file that is simultaneously owned and declared `exempt`.

**Naming discipline.** The required list mixes *declared capability modules* with *subsystems inside a module*. Where a requested name is not a declared capability, the section says so and names the declared owner module. Of the 27 required names, **none of "Provider / Web Runtime", "Computer Use", "Evidence", "Fleet", "GitHub", "Machine Identity", "Self Evolution", "Runtime Intelligence", "Adaptive Learning", "Self Cognition", "Self Diagnosis", "Self Case Record", "UI", "Qualification" or "Installer / Bootstrap" exists as a declared capability module**; each is a surface owned by one or more of the 27 declared modules. The remaining 12 required names (`Commander`, `Scheduler`, `Task State / Task Ledger`, `Runtime Registry`, `Role Router`, `Budget Manager`, `Context Manager`, `Execution Gate`, `Engineering`, `Research`, `Knowledge`, `Persistence / Event Journal`) are likewise subsystem-level except `Engineering`, `Research`, `Knowledge` and `Persistence`, which are declared modules.

---

## Summary table

`Lat.` = direct lateral (capability → capability) file-level imports observed, excluding kernel-mediated access. `RT` = root-trust relevance (touches `trust-policy/`, `electron/credential-boundary/`, `electron/promotion-gate/`, `src/shared/root-authority/`, qualification semantics, trust epoch, or required-check declaration).

| # | Capability | Owner module (declared) | Implementation root | Lat.? | RT? |
|---|---|---|---|---|---|
| 1 | Commander | `tenx` | `electron/commander/main-commander.ts` (+ 38 siblings) | yes (→ engineering, knowledge, providers, research, security, status, tasks, theme, workspace) | no |
| 2 | Scheduler | `tenx` | `electron/commander/scheduler.ts`, `recovery-scheduler.ts` | yes (→ providers: `RuntimeAdapter` type) | no |
| 3 | Task State / Task Ledger | `tasks` (+`task-creation`); ledger files `tenx`-owned by path | `electron/commander/task-ledger.ts`, `task-state-machine.ts`, `electron/tasks/`, `electron/bootstrap/task-*-ipc.ts` | yes (tasks → tenx) | no |
| 4 | Runtime Registry | `tenx` | `electron/commander/runtime-registry.ts` | none observed | no |
| 5 | Role Router | `tenx` | `electron/commander/role-router.ts` | none observed | no |
| 6 | Budget Manager | `tenx` | `electron/commander/{budget-manager,token-budget-manager,state-budget,storage-budget,resource-controller}.ts` | none observed | no |
| 7 | Context Manager | `tenx` | `electron/commander/context-manager.ts` | none observed | no |
| 8 | Execution Gate | `tenx` | `electron/commander/execution-gate.ts` | yes (runtime/capability → tenx, type-only) | partial — named boundary, no trust-policy read |
| 9 | Provider / Web Runtime | `providers` (kernel) | `electron/runtimes/`, `electron/adapters/`, `electron/provider-*.ts`, `src/shared/provider-*.ts` | yes (→ automation, attachments, engineering, identity, research; ← research, attachments) | no |
| 10 | Computer Use | `providers` (kernel) | `electron/computer/`, `src/shared/computer-recovery.ts` | yes (→ tasks, → engineering) | no |
| 11 | Engineering | `engineering` | `electron/engineering/` (50), `electron/self-engineering/` (3), 20 `src/shared/*` | yes (12 out / 9 in) | **yes** |
| 12 | Research | `research` | `electron/research/`, `electron/ingestion/`, `electron/protocols/`, 17 `src/shared/research-*.ts` | yes (9 out / 3 in) | indirect (`tests/acceptance/**` on surface) |
| 13 | Evidence | `status` + `host-status` + `research` (no single owner; **not a declared capability**) | `electron/evidence-engine.ts`, `src/shared/evidence-ledger.ts`, `electron/host/evidence-inspector.ts`, `electron/research/evidence/` | yes | **yes** (`acceptance-contracts.ts`, `acceptance-evidence.ts` are root-trust surface) |
| 14 | Knowledge | `knowledge` | `electron/knowledge/`, `electron/bootstrap/knowledge.ts`, 14 `src/shared/knowledge-*.ts` | yes (10 out / 7 in) | no (its `data-retention.ts` protects `trust-policy` from GC) |
| 15 | Fleet | `tenx` (+`node`) | `electron/fleet/`, `electron/tenx/{fleet,network,node-identity,task-lease,session}*`, `src/shared/fleet.ts`, `src/shared/tenx/{fleet,network,node,session}.ts` | yes (node → tenx, tenx → security) | no |
| 16 | GitHub | `security` (+`providers`, `promotion`) | `electron/github/`, `src/shared/github-url.ts`, `electron/input/github-resolver.ts` | yes (tenx → security; providers → security) | **yes** |
| 17 | Machine Identity | `security` | `src/shared/github-machine.ts`, `electron/credential-boundary/`, `electron/github/machine-identity-layout.ts` | yes (promotion → security) | **yes** (highest) |
| 18 | Self Evolution | `promotion` | `electron/self-evolution/`, `electron/promotion-gate/`, `electron/stable-candidate/`, `src/shared/autonomous-evolution-*.ts` | yes (promotion → engineering, security; engineering → promotion) | **yes** (highest) |
| 19 | Runtime Intelligence | `tenx` | `electron/runtime-intelligence/`, `src/shared/runtime-intelligence/` (27) | yes (→ status, → learning, → security) | no (boundary test asserts PRODUCT_SURFACE) |
| 20 | Adaptive Learning | `learning` | `electron/learning/` (23) | yes (← status; → providers via `providers`-owned contracts) | no |
| 21 | Self Cognition | `status` | `electron/self-cognition/facts.ts`, `src/shared/self-cognition/` | yes (→ promotion, → security) | **yes** (consumes the trust classifier) |
| 22 | Self Diagnosis | `status` (directory) + `engineering` (file) — **real split** | `src/shared/self-diagnosis/`, `electron/self-diagnosis/sources.ts`, `src/shared/self-diagnosis.ts`, `electron/self-engineering/diagnosis.ts` | yes | partial (`OWNER_ONLY_AUTHORITIES` names ROOT_TRUST_SURFACE) |
| 23 | Self Case Record | `status` | `electron/self-case-record/case-store.ts`, `src/shared/self-case-record/` | intra-module; → persistence (`hash.ts`) | boundary discipline only |
| 24 | UI | **exempt** (`src/renderer/**`), theme files → `theme` | `src/renderer/`, `electron/theme/`, `src/shared/theme*.ts`, `ui-surface*.ts` | no | no |
| 25 | Persistence / Event Journal | `persistence` + `state-core` (both kernel) | `electron/state-core/` (14), `electron/store.ts`, `electron/history-repository.ts`, `electron/bootstrap/{persistence,state-core}.ts` | yes (kernel; 12 out / 15 in) | no |
| 26 | Qualification | **undeclared** | `vitest.qualification.config.mjs`, `vitest.tiers.mjs`, `.github/workflows/`, `scripts/qualification-corpus-provenance.cjs`, `scripts/platform-certificate.cjs` | yes (→ `runtime` compiled modules; reads `trust-policy/`) | **yes** (central) |
| 27 | Installer / Bootstrap | `runtime` (bootstrap paths); `installer/**` **undeclared** | `installer/windows/`, `scripts/package-{installer,portable}.cjs`, `electron/bootstrap/` (26), `electron/main.ts` | yes (main.ts → every boot module) | partial (`src/shared/bootstrap-audit.ts` is on the surface) |

---

# Detailed capability sections

## 1. Commander

- **Current implementation location:** `electron/commander/main-commander.ts` (965 lines, `MainCommander`, `CommanderTaskInput`, `SelfEvolutionHost`) plus 38 siblings in `electron/commander/` including `plan-compiler.ts`, `plan-runner.ts`, `execution-supervisor.ts`, `task-finalizer.ts`, `task-policy.ts`, `workbook-dispatch.ts`, `workbook-production.ts`, `interruption.ts`, `event-bus.ts`, `degraded-controller.ts`, `continuation-router.ts`, `continuation-waker.ts`, `human-guidance-gate.ts`, `progress-recorder.ts`, `provider-dispatch-guard.ts`, `provider-session-registry.ts`, `runtime-policy.ts`, `web-recovery.ts`, `verification-collector.ts`.
- **Owner module:** `tenx` — `config/capabilities/tenx.yaml` (kind: feature, provides `tenx.artifact@1`, `tenx.network@1`, `modules: []`, `bootModules: []`); `config/capability-modules.json` owns `electron/commander` under `tenx`.
- **Inputs:** `CommanderTaskInput` (title/objective/providerIds/appMode/transports/budget/verification/conversationPolicy); `RuntimeRequest`; `WorkBookDispatchRequest`; `RecoveryWakeup`; injected `StateStore`, `TaskLedger`, `RuntimeRegistry`, `RoleRouter`, `BudgetManager`, `ContextManager`, `ExecutionGate`, `Scheduler`, `RecoveryScheduler`, `CircuitBreaker`, `DomainEventBus`, `SoftwareLeaseRegistry`, `SelfEvolutionHost`.
- **Outputs:** `BossTask`; `RuntimeResult`; `TaskIR`/`GraphResult`; `TaskExecutionResult`; `WorkDispatchOutcome` (`NOT_WORKBOOK|REUSED|BLOCKED|ANALYSIS_ONLY|NO_AUTO_RUN|DISPATCHED|RECOVERY_WAITING|FAILED`); `ExecutionRecord`; `DegradationState`; `EngineeringLoopSummary`; domain events `WORKER_COMPLETED|WORKER_FAILED|DEPENDENCY_READY`.
- **State ownership:** no declared namespace. In-process maps `planExecutions`, `nativeExecutions`, `finalizers`, `mergeCoordinator`; writes ledger fields `nextAction`, `verificationState`, `executedStages`, `policy`, `degradation`, `workspace`, `jobs`.
- **Permissions:** `desktopMutationGate` (`src/shared/permission.ts`) fails closed before desktop ops; software-lease `canAccess`/`acquire`/`release`; `workbook-dispatch.ts` calls `changeAllowed("core.security.boundary", token, …)` from `src/shared/guardian.ts`; `provider-dispatch-guard.ts` enforces 1–5 providers; `ExecutionGate` (constructed with **no** authorizer — see §8).
- **Dependencies:** `RuntimeRegistry`, `RoleRouter`, `Scheduler`, `BudgetManager`, `ContextManager`, `ExecutionGate`, `TaskLedger`, `RecoveryScheduler`, `CircuitBreaker`, `StateStore`; `src/shared/{task-ir,permission,guardian,result-validator,capability-router,capability-graph,execution,policy,provider-state,session-state}.ts`.
- **Consumers:** `electron/main.ts:910` (constructs it), `electron/bootstrap/dispatch-ipc.ts`, `electron/bootstrap/task-creation-ipc.ts`, `electron/host/soak-harness.ts`, `electron/host/fault-lab.ts`, `electron/platform/coordination-recorder.ts`, `electron/runtime-intelligence/live-capture.ts`.
- **Failure domain:** task-scoped. Plan-compile/verify/gate failures mark `REPAIR_OR_REPLAN` + `waiting`; ledger errors throw `"Plan execution requires durable ledger"` / `"Unknown task"`; errors propagate to `bootstrap/task-state-ipc.ts`, which records recovery state (never swallowed).
- **Tests:** `tests/unit/tenx-phase-10a..10r.test.ts`, `tests/unit/conversation-policy.test.ts`, `tests/unit/verification-contract.test.ts`, `tests/unit/execution-fault-injection.test.ts`, `tests/unit/multi-fault-isolation.test.ts`, `tests/unit/workbook-production.test.ts`, `tests/unit/workbook-dispatch.test.ts`, `tests/unit/web-recovery-r6.test.ts`, `tests/unit/degraded-controller-battery.test.ts`, `tests/acceptance/workbook-production-acceptance.test.ts`, `tests/acceptance/execution-plan.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:workbook`, `acceptance:architecture`, `acceptance:plan`, `acceptance:requirements`, `acceptance:bootstrap-completion`, `test:postbuild` (for `tests/acceptance/architecture-discovery.test.ts`).
- **Can be disabled?:** UNKNOWN — no flag or kill switch in `main-commander.ts`; `MainCommander` is constructed unconditionally at `electron/main.ts:910`.
- **Can be removed?:** No — every task path depends on it; all three task IPC boot modules call it.
- **Can be replaced?:** Partially — `PlanCompiler`/`PlanRunner`/`Scheduler`/`DegradedController` are constructor-injected; `MainCommander` itself has no factory seam.
- **Direct lateral dependencies?:** yes — `tenx → engineering` (`main-commander.ts` → `engineering/native-tools`, `merge-coordinator`, `workspace`, `proposal-runner`), `tenx → knowledge` (`context-manager.ts` → `src/shared/knowledge.ts`), `tenx → providers` (`budget-manager.ts`, `circuit-breaker.ts`, `degraded-controller.ts`, `event-bus.ts` → `../runtimes/runtime`), `tenx → research` (`workbook-dispatch.ts` → `electron/ingestion/*`), `tenx → security` (`main-commander.ts` → `src/shared/permission.ts`), `tenx → tasks`, `tenx → theme`, `tenx → workspace`.
- **Shared mutable state?:** yes — the single `TaskLedger` is shared with `StateStore` (`electron/bootstrap/persistence.ts:132`; comment at :45–47 says one ledger or read-modify-write cycles race with "Stale task checkpoint"); per-task in-process maps serialize execution.
- **Root-trust relevance?:** No. Grep for `trust-policy|trust-epoch|credential-boundary|promotion-gate|qualification|required-check` over `electron/commander/*.ts` returns no matches.

## 2. Scheduler

- **Current implementation location:** `electron/commander/scheduler.ts` (`Scheduler`, `DispatchPolicy`, `runBatch`, `executeWithTimeout`) and `electron/commander/recovery-scheduler.ts` (`RecoveryScheduler`, `RecoveryWakeup`, `RecoveryResult`). Related but separate (tenx-owned): `electron/tenx/dynamic-scheduler.ts`, `src/shared/tenx/scheduler.ts`.
- **Owner module:** `tenx`.
- **Inputs:** `ScheduledJob { request: RuntimeRequest; candidates: RuntimeAdapter[] }`; `DispatchPolicy { maxParallel, timeoutMs, maxRetries, allowFallback, requireAll, minSuccess? }`; `RecoveryScheduler.schedule({id,taskId,kind,retryAt,payload})`, `register(kind, handler)`, `resumeTask`, `runDue(now)`, `DomainEventBus` subscription to `DEPENDENCY_READY`.
- **Outputs:** `RuntimeResult`; `ScheduleBatchResult { results, state: "READY_TO_COMMIT"|"RECONCILIATION_REQUIRED", successCount }`; persisted `RecoveryWakeup[]`; `RecoveryResult`; `DEPENDENCY_READY` event.
- **State ownership:** `Scheduler` is in-memory only. `RecoveryScheduler` persists its wakeup list to an injected file wired to the **`recovery`** namespace, owned by `automation` (`electron/bootstrap/automation.ts`); `electron/main.ts:1171–1172` reads `resumeRecovery`/`waitingRetryTimes`.
- **Permissions:** none in either file — eligibility is delegated to the caller's `candidates` list.
- **Dependencies:** `scheduler.ts` imports only `RuntimeAdapter`/`RuntimeRequest`/`RuntimeResult` from `../runtimes/runtime`; `recovery-scheduler.ts` imports `./durable-json` and the `DomainEventBus` type.
- **Consumers:** `electron/commander/execution-supervisor.ts`, `electron/commander/main-commander.ts`, `electron/bootstrap/automation.ts`, `electron/commander/web-recovery.ts`, `electron/bootstrap/task-state-ipc.ts`, `electron/host/soak-harness.ts`, `electron/host/host-observer-collector.ts`, `electron/main.ts`.
- **Failure domain:** per-dispatch. Timeouts become `RETRYABLE_FAILURE { code: "TIMEOUT" }`; `runBatch` converts rejections into `PromiseSettledResult` and reports `RECONCILIATION_REQUIRED` rather than throwing; `runDue` catches handler throws and backs off `min(60000, 2000 * 2**attempts)`, pausing after 3 attempts with `"Recovery attempts exhausted"`.
- **Tests:** `tests/unit/recovery-scheduler.test.ts`, `tests/unit/recovery-resume-battery.test.ts`, `tests/unit/execution-fault-injection.test.ts`, `tests/unit/multi-fault-isolation.test.ts`, `tests/unit/phase-j-scenarios.test.ts`, `tests/unit/ui-isolation.test.ts`, `tests/unit/platform/restart-recovery.test.ts`, `tests/unit/web-recovery-r6.test.ts`.
- **Acceptance gates:** `typecheck`, `test`; `acceptance:self-healing` drives recovery. No scheduler-specific script.
- **Can be disabled?:** UNKNOWN — no flag. `dispose()`/`start()` exist and `main.ts:1457` calls `dispose()`.
- **Can be removed?:** No — `ExecutionSupervisor` and `MainCommander.dispatchRole` depend on `Scheduler`; `recoveryScheduler` is a required binding in `electron/main.ts`.
- **Can be replaced?:** Yes for `Scheduler` (constructor-injected with a default); `RecoveryScheduler` is a concrete class constructed in `electron/bootstrap/automation.ts`.
- **Direct lateral dependencies?:** `tenx → providers`, type-only (`../runtimes/runtime`).
- **Shared mutable state?:** yes — `RecoveryScheduler.records`/`handlers` plus its JSON file, single instance shared by `bootstrap/automation.ts`, `web-recovery.ts`, `main.ts`; `runDue` uses a single-flight guard.
- **Root-trust relevance?:** No.

## 3. Task State / Task Ledger

- **Current implementation location:** `electron/commander/task-ledger.ts` (`TaskLedger`, `TaskLedgerRecord`, `Consumption`, `WorkerSession`, `LedgerJob`, `ExecutedStage`), `electron/commander/task-state-machine.ts` (`TaskStateMachine`, `transitions`), `electron/tasks/task-inputs.ts`, `electron/bootstrap/task-lifecycle-ipc.ts`, `electron/bootstrap/task-state-ipc.ts`, `electron/bootstrap/task-creation-ipc.ts`. The `tasks` surface lives in 29 files per `capability-modules.json`.
- **Owner module:** declared module **`tasks`** (`config/capabilities/tasks.yaml`, provides `task.lifecycle@1`, `task.state@1`) plus **`task-creation`** (`task.creation@1`). **Note the split:** `electron/commander/task-ledger.ts` and `task-state-machine.ts` are owned by `tenx` by path (`capability-modules.json`), even though the ledger is the durable `tasks` store.
- **Inputs:** `taskId`, `objective`, `constraints[]`, `TaskBudgetOptions`, `ReproductionSnapshot`, `Interruption`; `TaskStatus`; IPC `CreateTaskInput`, `InputRefSources`, `TaskLifecycleSurface`, `TaskStateEventlessSurface`.
- **Outputs:** `TaskLedgerRecord` (schemaVersion 1: `completedSteps`, `currentStep`, `pendingSteps`, `modifiedFiles`, `verificationState`, `failureHistory`, `sessions`, `jobs`, `executionState`, `executedStages`, `policy`, `reviewFindings`); `TaskStatus` transitions; IPC channel sets `TASK_LIFECYCLE_IPC_CHANNELS` (8), `TASK_STATE_IPC_CHANNELS` (4), `TASK_CREATION_IPC_CHANNELS` (1).
- **State ownership:** the **`tasks`** namespace (declared owner: `persistence`). Concrete files `<dataRoot>/tasks/<validId(taskId)>/checkpoints/<8-digit>.json` and `.../repro.json` (`task-ledger.ts:124,142,200`); `electron/bootstrap/persistence.ts:131` opens it and :132 hands the same ledger to `StateStore`.
- **Permissions:** `validId` regex `/^[a-zA-Z0-9][a-zA-Z0-9_-]{0,127}$/` guards every path (`durable-json.ts:30`); IPC uses `requireTask` (`Unknown task: <id>`) and `MAX_ACTIVE_PROVIDERS`. No trust/capability check.
- **Dependencies:** `./durable-json`, `./interruption`, `./storage-budget`, `../repro-snapshot`, `src/shared/{config-layering,session-state,contracts,input-object}`; IPC modules also `./boot-module`, `./shared/require-provider`, `../tasks/task-inputs`, `../commander/{workbook-dispatch,main-commander}`, `../evidence-engine`, `src/shared/provider-policy`.
- **Consumers:** `electron/bootstrap/persistence.ts`, `electron/store.ts`, `electron/main.ts`, `electron/engineering/{engineering-runtime,microtask-runtime}.ts`, `electron/host/{fault-lab,host-observer-collector,soak-harness}.ts`, `electron/platform/coordination-recorder.ts`, `electron/runtime-intelligence/live-capture.ts`, `electron/commander/{execution-supervisor,provider-session-registry,degraded-controller,plan-runner,task-policy,task-finalizer}.ts`.
- **Failure domain:** task-scoped, fail-closed. `load` throws `"Invalid task checkpoint"` on a damaged newest generation rather than rolling back; `save` throws `"Stale task checkpoint"`; `update` throws `"Unknown ledger task"`; `TaskStateMachine.transition` throws `Invalid task transition: X -> Y`.
- **Tests:** `tests/unit/task-ledger.test.ts` (stale-write rejection, idempotent create + purge, durable timestamps), `tests/unit/task-state-machine.test.ts` (transition table + unapproved-artifact gate), `tests/unit/workbook-repair-batch3.test.ts`, `tests/unit/action-readiness.test.ts`, `tests/unit/result-validator.test.ts`, `tests/acceptance/{workbook-production-acceptance,execution-plan,requirements-graph}.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:workbook`, `acceptance:plan`, `acceptance:requirements`, `acceptance:evidence-integrity`, `acceptance:owner-ledger`, `acceptance:bootstrap-completion`.
- **Can be disabled?:** No — the ledger is constructed once in `electron/bootstrap/persistence.ts` and shared; the three IPC modules are boot modules registered by `main.ts`. Degradation flips `health()` to `DEGRADED` instead.
- **Can be removed?:** No — the ledger is the durable record every recovery/finalization path reads; the IPC channels are the renderer's only entry points.
- **Can be replaced?:** Yes behind injection, but the on-disk `checkpoints/<8-digit>.json` + `repro.json` layout and `schemaVersion: 1` are a compatibility contract (`load` rejects anything else).
- **Direct lateral dependencies?:** yes — `tasks → tenx`: `electron/tasks/task-inputs.ts:1` imports `hydrateWorkBookAttachmentPaths` from `../commander/workbook-dispatch`; `bootstrap/task-creation-ipc.ts:4,6` imports from `../commander/*`; `task-state-ipc.ts`/`task-lifecycle-ipc.ts` drive `MainCommander` through a structural surface.
- **Shared mutable state?:** yes — one `TaskLedger` shared by `StateStore` and the commander (`persistence.ts:45–47` documents this as the fix for racing "Stale task checkpoint"); `state.json` and the checkpoint directory are both written.
- **Root-trust relevance?:** No.

## 4. Runtime Registry

- **Current implementation location:** `electron/commander/runtime-registry.ts` (`RuntimeRegistry`).
- **Owner module:** `tenx`.
- **Inputs:** `RuntimeAdapter` (with optional `compatibility`), `RuntimeId`, `RuntimeCapability`.
- **Outputs:** `RuntimeAdapter | undefined` (`get`), `RuntimeAdapter[]` (`list`, `listAvailable`, `findByCapability`), `RuntimeHealth[]` (`refreshHealth`), `RuntimeHealth | undefined`, `boolean` (`unregister`).
- **State ownership:** in-memory only — `runtimes: Map<RuntimeId, RuntimeAdapter>`, `health: Map<RuntimeId, RuntimeHealth>`. No durable namespace.
- **Permissions:** none — visibility is capability-based via `findByCapability` and `isRuntimeAvailable(health.availability)`.
- **Dependencies:** `../runtimes/runtime`; `src/shared/compatibility` (`compatibilityIssue`). Note `src/shared/compatibility.ts` is the one file in the `exempt` list (see *Gaps*).
- **Consumers:** `electron/main.ts` (constructs at :800, passes to `MainCommander`/`RoleRouter`), `electron/bootstrap/providers.ts`, `electron/host/fault-lab.ts`, `electron/platform/external-compatibility.ts`, `electron/commander/{role-router,main-commander}.ts`.
- **Failure domain:** process-wide registry. `register` throws `Duplicate runtime: <id>` and refuses an out-of-contract adapter via `compatibilityIssue`; `refreshHealth` isolates each probe with `Promise.allSettled` + 15000 ms timeout and records `availability: "DOWN"` with the reason; `require` throws `Unknown runtime: <id>`.
- **Tests:** `tests/unit/bootstrap-providers.test.ts`, `tests/unit/platform/external-compatibility.test.ts`, `tests/unit/engine-phase-0.test.ts`, `tests/unit/engine-acceptance.test.ts`, `tests/unit/standalone-node.test.ts`, `tests/unit/conversation-policy.test.ts`, `tests/unit/verification-contract.test.ts`, `tests/unit/platform/provider-usage.test.ts`. No file named for it; it is exercised through consumers.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:architecture`. No registry-specific script.
- **Can be disabled?:** UNKNOWN — no flag.
- **Can be removed?:** No — required constructor argument of `RoleRouter` and `MainCommander`.
- **Can be replaced?:** Yes — plain class, injectable; consumers import the type, not the instance.
- **Direct lateral dependencies?:** none observed.
- **Shared mutable state?:** yes — one instance's live adapter + health maps are read by `MainCommander`, `RoleRouter`, `host/fault-lab.ts`, `platform/external-compatibility.ts`.
- **Root-trust relevance?:** No.

## 5. Role Router

- **Current implementation location:** `electron/commander/role-router.ts` (`RoleRouter`, `RoleId = "planner"|"researcher"|"reviewer"|"synthesizer"|"coder"|"validator"|"critic"`, `RoleRoutingRequest`, `RuntimeCandidate`, `brandKind`, `kindRank`, `score`, `reasonFor`).
- **Owner module:** `tenx`.
- **Inputs:** `RoleRoutingRequest { role, requiredCapabilities?, capabilityTokens?, preferredRuntimes?, excludedRuntimes?, pinnedRuntime?, allowFallback? }`; injected `RuntimeRegistry`, `BudgetManager`, optional `ResourceController`, optional `AdaptiveReranker` + `RoleRoutingAdaptiveContext`.
- **Outputs:** `RuntimeCandidate[] { runtimeId, rank, reason }`; `CapabilityResolution`; `RoleRoutingOutcome | undefined` (`adaptiveOutcome`).
- **State ownership:** in-memory only — `lastOutcome`. No persistence.
- **Permissions:** hard eligibility filter in `route` — excluded ids, `budgets.eligible(runtime.id)`, every resolved capability present in `runtime.capabilities.roles`, `isRuntimeAvailable(health)`. The adaptive reranker may only reorder the approved set (re-validates the multiset, refuses to displace an explicit pin, any throw falls back to static order).
- **Dependencies:** `./resource-controller`, `./runtime-registry`, `./budget-manager` (types), `../runtimes/runtime`, `src/shared/capability-graph`, `src/shared/cheapest-execution`, `src/shared/adaptive-routing` (types).
- **Consumers:** `electron/main.ts:910` (`new RoleRouter(runtimeRegistry, budgetManager, resourceController)`), `electron/commander/main-commander.ts` (`dispatchRole`), `electron/host/fault-lab.ts`, `electron/protocols/protocol.ts` (type `RoleId`).
- **Failure domain:** routing only — an ineligible runtime is silently absent (no throw); `route` returns `[]` when nothing qualifies; adaptive failures are contained by `catch { return candidates; }` (A27).
- **Tests:** `tests/unit/engine-phase-6.test.ts`, `tests/unit/engine-phase-9.test.ts`, `tests/unit/engine-acceptance.test.ts`, `tests/unit/budget-predicate-purity.test.ts`, `tests/unit/capability-router.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:architecture`. No router-specific script.
- **Can be disabled?:** UNKNOWN — no flag.
- **Can be removed?:** No — required constructor parameter of `MainCommander`; `RoleId` is imported by `electron/protocols/protocol.ts`.
- **Can be replaced?:** Yes — constructor-injected; the adaptive reranker is itself an optional replaceable seam.
- **Direct lateral dependencies?:** none observed.
- **Shared mutable state?:** minimal — `lastOutcome` written on every `route`, read by `adaptiveOutcome()`; the shared `RuntimeRegistry` health map and `BudgetManager` states are read, not owned.
- **Root-trust relevance?:** No, but note: `RoleRouter` is the seam through which the Adaptive Learning reranker would act, and at `electron/main.ts:910` it is built **with no reranker**, so adaptive routing is inert in production (see §20).

## 6. Budget Manager

- **Current implementation location:** `electron/commander/budget-manager.ts` (`BudgetManager`, `RuntimeBudgetLevel`, `RuntimeBudgetSource`, `RuntimeBudgetState`), `token-budget-manager.ts` (`TokenBudgetManager`, `STAGE_SOFT_LIMITS`), `state-budget.ts` (`applyStateStorageBudget`), `storage-budget.ts` (`pruneTaskCheckpoints`, `RetentionPolicy`, `PruneReport`), `resource-controller.ts` (`ResourceController`, `ScopedMemory`, `DegradedMode`).
- **Owner module:** `tenx`.
- **Inputs:** runtime failure messages + optional JSON file; `TokenSpend { taskId, stage, model, inputTokens, cacheHitTokens, cacheMissTokens, outputTokens, estimatedCost, recordedAt }`; `AppSnapshot` + `StorageBudgetPolicy`; `RetentionPolicy`; `(backend, passed, modelCalls, runtimeMs)`.
- **Outputs:** `RuntimeBudgetState`/`eligible`/`reconcile`; `BudgetLedgerEntry`/`spendFor`/`BudgetStatus`; `{ snapshot, report: LifecyclePruneReport }`; `PruneReport`; `ResourceController.score(backend)`; `ScopedMemory` entries.
- **State ownership:** **`runtime-budget`** namespace (`persistence.yaml`) — `electron/bootstrap/persistence.ts:145`; **`runtime-resources`** — :174; prunes the **`tasks`** checkpoint tree from `task-ledger.ts:145` every 20th revision; `TokenBudgetManager` persists `{schemaVersion:1, entries}` to its constructor path; `state-budget.ts` mutates the injected `AppSnapshot` (state.json arrays); `ScopedMemory` writes `<ledger.root>/../memory/<scope>/<owner>/<key>.json`.
- **Permissions:** none check capabilities or trust. Structural guards only: `validId` on memory scope/owner/key, `"Memory entry exceeds context budget"` above 50000 chars, `"Invalid runtime budget state"`, `"Invalid usage evidence"`, `"Retention must keep at least 5 generations"`, and `applyStateStorageBudget` refuses all deletion unless `policy.userAuthorizedDelete === true`.
- **Dependencies:** `./durable-json`; `../runtimes/runtime` (type); `src/shared/data-lifecycle`; `src/shared/contracts` (type `AppSnapshot`).
- **Consumers:** `electron/bootstrap/persistence.ts`, `electron/main.ts`, `electron/commander/{role-router,degraded-controller,execution-supervisor,web-recovery,main-commander}.ts`, `electron/store.ts`, `electron/commander/task-ledger.ts`, `electron/host/{fault-lab,host-observer-collector,soak-harness}.ts`.
- **Failure domain:** per-runtime budget + per-store retention. `observeFailure` classifies quota/rate-limit text into `EXHAUSTED`/`LOW` (else `undefined`); `eligible` is a pure predicate; `reconcile` releases in memory without persisting; `writeJson` failures from `update`/`observeSuccess` escape to the caller; prune fails closed on a missing newest generation.
- **Tests:** `tests/unit/budget-predicate-purity.test.ts` (8 cases), `tests/unit/degraded-controller-battery.test.ts`, `tests/unit/engine-phase-6.test.ts`, `tests/unit/tenx-phase-10*.test.ts`. `TokenBudgetManager` has **no** test file in the catalogue. `resource-controller.ts` has no test importing it by name.
- **Acceptance gates:** `typecheck`, `test`, `test:slow`; `data:lifecycle-report` exercises `src/shared/data-lifecycle`. No budget-specific script.
- **Can be disabled?:** Partially — `BudgetManager` accepts an undefined `file` (memory-only); `MainCommander`'s `resources` is optional. `BudgetManager` itself is a required constructor argument of `MainCommander`/`RoleRouter`/`DegradedController`.
- **Can be removed?:** No for `BudgetManager`; **yes for `TokenBudgetManager`** — grep over `electron/`, `src/`, `tests/`, `scripts/` finds no importer at all beyond its own definition.
- **Can be replaced?:** Yes — all five are classes/functions behind injected parameters; `StorageBudgetPolicy`/`RetentionPolicy` are data.
- **Direct lateral dependencies?:** none observed (imports are `./durable-json`, a `../runtimes/runtime` type, and `src/shared/{data-lifecycle,contracts}`).
- **Shared mutable state?:** yes — one `BudgetManager.states` map shared by routing, degraded control, execution supervision, web recovery and the commander; `ResourceController.observations` shared by routing and `main-commander`; `state-budget.ts` rewrites the same `state.json` the store owns.
- **Root-trust relevance?:** No.

## 7. Context Manager

- **Current implementation location:** `electron/commander/context-manager.ts` (`ContextManager`, `TaskContext`, `cacheKeyFor`, `CONTEXT_SCHEMA = "task-contexts"`).
- **Owner module:** `tenx` by path; the durable namespace it writes is declared by `persistence` (`task-contexts`).
- **Inputs:** `TaskContext { taskId, objective, constraints, currentProtocol, currentRound, resolvedClaims, openDisputes, artifactRefs, summaries, executionHistory }`; `RoleId` + `roleInstructions`; optional `files`, `architecture`, `maxChars`; `ContextBudget { maxChars?, maxTokens?, maxArtifacts? }`; injected `knowledgeProvider` and `knowledgeSectionProvider`.
- **Outputs:** assembled prompt strings (`assemble`, `assembleStep`), `ContextCapsule` (`capsule`), content-addressed key `{ key, scope, fingerprint, level }`, `restoreDiagnostic()`.
- **State ownership:** **`task-contexts`** namespace; file `path.join(dataRoot, "task-contexts.json")` (`electron/bootstrap/persistence.ts:163`), persisted as a `VersionedEnvelope<TaskContext[]>` with `schema_id: "task-contexts"`; in-memory `contexts: Map`; `retainTaskIds` prunes to live task ids (called at `persistence.ts:166`).
- **Permissions:** none. Bounds are budgets: `maxChars` default 24000, `maxTokens * 4`, `maxArtifacts` default 20, knowledge gets one third of remaining; `assembleStep` slice caps 12000 dependency chars, 16000 per authorized file, 64000 total.
- **Dependencies:** `./schema-migration` (`readEnvelope`, `migrateJsonFile`), `src/shared/context-capsule`, `src/shared/contracts`, `src/shared/knowledge` (type), `./role-router` (type), `node:fs`/`path`/`crypto`.
- **Consumers:** `electron/bootstrap/persistence.ts`, `electron/main.ts:726,862,910`, `electron/commander/main-commander.ts`.
- **Failure domain:** context-only, fail-safe. A corrupt/unreadable file is caught, recorded in `restoreFailure`, warned, and the process continues with no contexts (canonical task state is never replaced); a missing context throws `Unknown task context: <id>`; `persist` is atomic (tmp + rename with `EXDEV/EEXIST/EPERM` fallback).
- **Tests:** `tests/unit/context-manager-restore.test.ts`, `tests/unit/context-knowledge-section.test.ts`, `tests/unit/schema-migration.test.ts`, `tests/unit/conversation-policy.test.ts`, `tests/unit/standalone-node.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:knowledge`, `acceptance:workbook`. No context-specific script.
- **Can be disabled?:** Partially — `filePath` is optional so it can run in-memory only; `persistence.ts` always supplies a path.
- **Can be removed?:** No — `MainCommander` requires it; removing it drops capsule/context assembly for every provider call.
- **Can be replaced?:** Yes — constructor accepts `filePath` and a knowledge provider; both providers are swappable at runtime. The `schema_id: "task-contexts"` v1 envelope is a compatibility contract.
- **Direct lateral dependencies?:** none observed.
- **Shared mutable state?:** yes — one instance's `contexts` map written by `MainCommander` and pruned by persistence lifecycle; the whole `task-contexts.json` file is rewritten on every `save`.
- **Root-trust relevance?:** No — `trust` appears only as a knowledge-entry field passed into the capsule (`context-manager.ts:130`).

## 8. Execution Gate

- **Current implementation location:** `electron/commander/execution-gate.ts` (`ExecutionGate`, `ExecutionProposal`, `ExecutionRecord`, `ExecutionAuthorizationContext`, `ExecutionAuthorizationOutcome`, `ExecutionDeniedError`, `ExecutionAuthorizationRecord`).
- **Owner module:** `tenx`; `electron/capability/integration/boundary-inventory.ts:51` lists `electron/commander/execution-gate.ts` as the one `mapped` high-privilege boundary.
- **Inputs:** `ExecutionProposal { id?, kind: "shell"|"filesystem"|"git"|"network", description, payload, originArtifactId? }`; `ExecutionGateOptions.authorizer?` (structurally typed, deliberately not imported from the capability layer); an executor callback at `execute` time.
- **Outputs:** `ExecutionRecord` (`status: PROPOSED|VALIDATED|APPROVAL_REQUIRED|APPROVED|EXECUTING|SUCCEEDED|FAILED`); the executor's `T`; `authorizationEnabled()`; `authorizationHistory()`; `ExecutionDeniedError`.
- **State ownership:** in-memory only — `records: Map`, `authorizations: ExecutionAuthorizationRecord[]`.
- **Permissions:** the gate **is** the permission mechanism. `validate` sets `APPROVAL_REQUIRED` whenever `originArtifactId` is present; `approve` only from `VALIDATED|APPROVAL_REQUIRED`; `execute` refuses anything not `APPROVED` (`"Execution is not approved"`), then asks the optional authorizer and throws `ExecutionDeniedError` on refusal. An authorizer that throws is recorded as `allowed: false` and denied (never fail-open).
- **Dependencies:** `node:crypto` (`randomUUID`) only — no import of the capability broker, trust policy or persistence.
- **Consumers:** `electron/main.ts:32,910`, `electron/commander/main-commander.ts` (`requestExecution`), `electron/capability/integration/execution-authorization.ts` (the wiring side of the authorizer seam).
- **Failure domain:** per-proposal. Unknown id throws `Unknown execution proposal: <id>`; invalid proposal throws `"Invalid execution proposal"`; wrong state throws `Cannot approve <status>` / `"Execution is not approved"`; executor failure sets `FAILED` with `String(error)` and rethrows.
- **Tests:** `tests/unit/task-state-machine.test.ts` (never executes an unapproved artifact-derived proposal), `tests/unit/capability/boundary-integration.test.ts`, `tests/unit/conversation-policy.test.ts`, `tests/unit/engineering-{goal-rollback,recovery,session-isolation}.test.ts`, `tests/unit/verification-contract.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `acceptance:root-hardening`, `acceptance:architecture`, `capability:surface`. No gate-specific script.
- **Can be disabled?:** Effectively already disabled in the shipped wiring — `electron/main.ts:910` constructs `new ExecutionGate()` with **no authorizer**, so `authorizationEnabled()` is false and the capability-authorization branch of `execute()` is dead. There is no config switch.
- **Can be removed?:** Not safely — required constructor argument of `MainCommander`; removal also deletes the unapproved-artifact refusal asserted by `tests/unit/task-state-machine.test.ts`.
- **Can be replaced?:** Yes — the authorizer is a structural function injected via `ExecutionGateOptions.authorizer`.
- **Direct lateral dependencies?:** yes, one direction only — `electron/capability/integration/execution-authorization.ts` (capability layer, owned by `runtime`) imports `ExecutionAuthorizationContext`/`Outcome` from `electron/commander/execution-gate` (owned by `tenx`). The gate itself imports nothing lateral.
- **Shared mutable state?:** minimal — per-instance `records`/`authorizations`; `main.ts` creates one instance for `MainCommander`. Payloads are `structuredClone`d on the way in and out.
- **Root-trust relevance?:** Partial. It is the boundary the capability layer authorizes through, and `boundary-inventory.ts` names it as a security boundary, but it touches no `trust-policy/` file, `credential-boundary/`, `promotion-gate/`, or required-check declaration.

## 9. Provider / Web Runtime

> **NOT a declared capability.** The declared owner module is **`providers`** (`config/capabilities/providers.yaml`, kind **kernel**, `health.critical: true`, provides `provider.pool@1`, `provider.policies@1`, `provider.registry@1`).

- **Owner module:** `providers` (`config/capabilities/providers.yaml`, kind **kernel**, `health.critical: true`). There is no capability literally named "Provider / Web Runtime".
- **Current implementation location:** `electron/runtimes/` (`runtime.ts`, `native-api-runtime.ts`, `unsupported-runtime.ts`, `codex/codex-cli-runtime.ts`, `web/provider-runtime-adapter.ts`); `electron/adapters/{registry.ts,page-scripts.ts}`; `electron/{provider-api,provider-views,provider-automation,account-sessions,api-settings}.ts`; `electron/input/{provider-capability-registry,github-resolver}.ts`; `electron/bootstrap/{provider-ipc,provider-pool,providers}.ts`; `electron/software/{software-runtime,media-adapters}.ts`; 19 `src/shared/provider-*`/related contracts. 50 files total.
- **Owner-module exclusions (real):** `src/shared/provider-models.ts` and `electron/bootstrap/shared/require-provider.ts` are declared under **`runtime`**, not `providers`.
- **Inputs:** `RuntimeRequest { jobId, taskId, role, prompt, context, timeoutMs }`; `UpdateApiSettingInput`; `CustomProviderInput`/`ViewBounds` via `boss:*` IPC; `AdapterDefinition` selectors; `GithubTarget`; `SoftwareAction[]` + `PermissionManifest`.
- **Outputs:** `RuntimeResult { status, artifact, content, failure, metrics, usage }`; `RuntimeHealth`; `ApiCompletion` + `ProviderUsage`; live `WebContentsView` panes; `SoftwareObservation[]`/`SoftwareVerification`; `ProviderCapabilityProfile`; `CompatibilityVerdict`.
- **State ownership:** durable namespaces **`provider-capabilities`** and **`api-settings`**, both owned by `persistence` (`persistence.yaml:22–25`; written at `electron/bootstrap/persistence.ts:134,138`; asserted by `tests/unit/bootstrap-persistence.test.ts:43–44,71–73`). In-process: `ProviderViews.views/manualZoom/lastBounds/viewState`; `ProviderAutomation.monitors/baselines/stability/dispatching`.
- **Permissions:** `providers.yaml` declares `permissions: []`. Real gates elsewhere: `software-runtime.authorize` requires `manifestAllows(manifest, "side-effect", \`${adapter.id}:${action.capability}\`)`; provider panes open with `sandbox:true`/`contextIsolation:true`/`nodeIntegration:false` (`provider-views.ts:189–194`); `sanitizeViewLayout`; `assertReady` for API use.
- **Dependencies:** Electron (`BrowserWindow`/`WebContentsView`/`session`); `StateStore`; global `fetch`; `git-gateway`; `process-gateway`; `engineering/native-tools`; kernel `persistence` and `runtime`.
- **Consumers:** `electron/main.ts:6-7,21,59,93-97,979`; `electron/bootstrap/{provider-ipc,provider-pool,providers,settings-ipc,task-creation-ipc,dispatch-ipc,research}.ts`; `electron/commander/{web-recovery,main-commander,role-router}.ts`; `electron/platform/external-compatibility.ts:22`; `electron/computer/backends/provider-{dom,vision}-surface.ts`; `electron/input/attachment-router.ts:12`; `electron/learning/routing/adaptive-scorer.ts:15`.
- **Failure domain:** `providers.yaml health.critical: true`. Module health `DEGRADED` when `githubMachine.configured` is false (`bootstrap/providers.ts:187–193`); pool `DEGRADED` with no controller window (`provider-pool.ts:130–136`); external breakage degrades only the provider (`external-compatibility.ts:164–175`; `network-policy.ts:65` `DEGRADE_PROVIDER`); `UnsupportedRuntime` returns `UNSUPPORTED`/`PERMANENT_FAILURE`.
- **Tests:** 29 suites catalogued under `providers` — `tests/unit/{bootstrap-providers,bootstrap-provider-pool,bootstrap-ipc-modules,capability-router,computer-recovery,dom-page,login-scan,network-policy,provider-dom-surface,provider-page-repair,web-recovery-r6,execution-fault-injection,multi-fault-isolation,degraded-controller-battery,phase-j-scenarios,engine-acceptance,recovery-resume-battery,session-lifecycle,ui-isolation}.test.ts`, `tests/unit/engine-phase-{0,1,2,5,6,7,9,10,11}.test.ts`, `tests/unit/platform/{provider-usage,external-compatibility,provider-models}.test.ts`.
- **Acceptance gates:** `typecheck`, `test`, `test:platform-qualification`, `test:impact`, `test:catalogue:check`, `architecture:ratchet`, `acceptance:architecture`, `platform:certificate`, `acceptance:desktop-contract`.
- **Can be disabled?:** No as a module — `optional: []` and `health.critical: true` declare nothing optional. Per provider: API transport is off until enabled + keyed; panes may stay unopened; `UnsupportedRuntime` covers declared-but-unimplemented runtimes.
- **Can be removed?:** No — kernel-critical provision with unconditional registration (`bootstrap/providers.ts:162–178`; `provider-pool.ts:98–121` throws `"Main window was not created"`).
- **Can be replaced?:** Transport yes — `RuntimeAdapter` is an interface with 4 implementations plus declared compatibility windows checked at registration; adapter selectors are a data table; `ProviderRuntimeAdapter` requires ids prefixed `web:`. IPC channel names (`PROVIDER_IPC_CHANNELS`) are not cheap to change.
- **Direct lateral dependencies?:** yes — `providers → automation` (`bootstrap/provider-pool.ts` → `provider-automation.ts`), `→ attachments`, `→ engineering` (`native-api-runtime.ts:4` → `engineering/native-tools`), `→ identity` (`account-sessions.ts:7`), `→ promotion` (`src/shared/provider-intelligence.ts` → `behaviour-epoch.ts`), `→ security` (`computer/computer-service.ts` → `src/shared/permission.ts`), `→ tasks`, `→ tenx`, `→ workspace`.
- **Shared mutable state?:** yes — the single `StateStore` snapshot mutated by `ProviderAutomation`; per-instance maps; `api-settings.json` written by tmp-file + rename; `provider-capabilities.json`.
- **Root-trust relevance?:** No. API keys are encrypted by injected crypto and only a 4-char tail reaches the renderer (`api-settings.ts:25,35–43,54`).

## 10. Computer Use

> **NOT a declared capability.** Declared owner module: **`providers`** (kernel). `providers.yaml` itself lists only the three `provider-*` boot modules.

- **Owner module:** `providers` (kernel). There is no capability literally named "Computer Use".
- **Current implementation location:** `electron/computer/{computer-service.ts,perception-loop.ts,provider-page-repair.ts,semantic-runtime.ts,software-lease.ts}` + `electron/computer/backends/{dom-page,provider-dom-surface,provider-vision-surface,structured-apps,vision,vscode-cli,windows-ocr,windows-uia}.ts` + `src/shared/computer-recovery.ts`.
- **Inputs:** `SemanticAction { name, target, value, expected, timeoutMs }`; `ComputerOptions { visionSurface, authorizeVision, permissionForWorkspace, domPageSurface, vscodeExecutable, vscodeUserDataDir, readBrowser }`; `PerceptionRequirement`; `RepairPlan`; the task's `computer:<action>` allow-list; OCR image.
- **Outputs:** `SemanticResult { status: SUCCESS|UNSUPPORTED|FAILED|UNCERTAIN, evidence, message, backend }`; `PerceptionEpisode` (key frames only: before/after/error/final); `PageRepairOutcome { REPAIRED|UNCERTAIN|DENIED|UNSUPPORTED|FAILED }`; `RepairPlan`/`planSummary`; captured PNG paths; `SoftwareSession` leases.
- **State ownership:** **no durable namespace declared**. `SemanticRuntime.pending` is written to an injected `stateFile` JSON; `PerceptionLoop` writes `{schemaVersion:1, episode}` and restores it; `SoftwareLeaseRegistry.sessions` is an in-memory `Map`; a module-level `queues` `Map` is keyed by `stateFile`/symbol.
- **Permissions:** mutations require `computer:<action>` grants — `buildRepairPlan` returns `DENIED` without `read_page`/`click_control`/`enter_text`/`submit`; `permissionForWorkspace` resolves a workspace `PermissionManifest` for desktop side-effect gating; a vision click needs the injected `authorizeVision` (default `async () => false`); DOM scripts run inside sandboxed provider panes; `providers.yaml` declares `permissions: []`.
- **Dependencies:** Electron `WebContentsView.capturePage`/`sendInputEvent`; `node:child_process` spawn (`windows-uia.ts:101`, `windows-ocr.ts:12`); `powershell.exe` + `scripts/windows-ocr.ps1`; a PowerShell `UIAutomationClient`/`UIAutomationTypes` bridge; `electron/process/process-gateway`; `electron/engineering/native-tools`; `src/shared/action-readiness`. **No npm native/optional dependency** (`package.json` deps: `cross-env, electron, fflate, mammoth, pdfjs-dist, react, react-dom, yaml`).
- **Consumers:** `electron/commander/main-commander.ts:2–4`; `electron/main.ts:1–2,910`; `electron/software/software-runtime.ts:1` and `electron/bootstrap/automation.ts:8` (`SoftwareLeaseRegistry`); `electron/provider-automation.ts:3` (`SemanticRuntime`). **Not observed:** `perception-loop.ts` has no non-test importer; `createPageRepairExecutor` is referenced only by its test; `electron/main.ts:473` builds `WebRecovery` without the repair executor.
- **Failure domain:** per-action status rather than thrown errors. Backends tried native > dom > uia > structured > vision, returning `UNSUPPORTED` if none applies; an unverified mutation is recorded pending and the next mutation returns `UNCERTAIN`; `WindowsUiaBackend` is win32-only; `readWindowsOcr` throws off win32; an unverified vision click returns `UNCERTAIN`; provider panes fail closed when closed/crashed/ambiguous.
- **Tests:** `tests/unit/computer-recovery.test.ts` (§26 classification, `HUMAN_REQUIRED` for login/CAPTCHA, `DENIED` without grants, `UNCERTAIN` geometry), `tests/unit/dom-page.test.ts`, `tests/unit/provider-dom-surface.test.ts`, `tests/unit/provider-page-repair.test.ts`, plus `degraded-controller-battery`, `execution-fault-injection`, `multi-fault-isolation`, `web-recovery-r6`, `recovery-resume-battery`, `ui-isolation`, `engine-*`.
- **Acceptance gates:** `typecheck`, `test`, `test:platform-qualification`, `acceptance:desktop-contract`, `acceptance:architecture`, `architecture:ratchet`, `test:impact`.
- **Can be disabled?:** Yes at the wiring seam — `domPageSurface`/`visionSurface` are optional and the DOM/vision backends are only appended when supplied; `WindowsUiaBackend` self-gates on `process.platform`. No feature flag or env var was observed.
- **Can be removed?:** Not as a whole — `computer-service`, `semantic-runtime` and `software-lease` are imported in production. Individually removable without observed production impact: `perception-loop.ts`, `provider-page-repair.ts`.
- **Can be replaced?:** Yes behind `SemanticBackend` and the injected `DomPageSurface`/`VisionSurface`; `createComputerRuntime` composes the backend list; OCR is injectable as `VisionBackend`'s dependency.
- **Direct lateral dependencies?:** yes — `providers → tasks` (`backends/dom-page.ts:2` → `src/shared/action-readiness`, declared under `tasks`); `providers → engineering` (`backends/structured-apps.ts:5` → `engineering/native-tools`). Otherwise only kernel and same-module files.
- **Shared mutable state?:** yes — the module-level `queues` map is shared by every `SemanticRuntime` using the same `stateFile`; the persisted `pending` map is re-read by both the entry and `executeLocked` paths; one `SoftwareLeaseRegistry` shared by `software-runtime` and `bootstrap/automation`; perception episodes append to a shared `stateFile`.
- **Root-trust relevance?:** No. Grep for `trust-policy|credential-boundary|promotion-gate|trustEpoch|root-authority` over `electron/computer/*` and `src/shared/computer-recovery.ts` returns no matches; `computer-recovery.ts:76` only lists the words "credential"/"权限" inside the `HUMAN_PHRASES` page-failure text array.

## 11. Engineering

- **Current implementation location:** `electron/engineering/` (50 files: `engineering-runtime.ts`, `engineering-session.ts`, `engineering-loop-driver.ts`, `autonomous-evolution-{identity,runner,surface}.ts`, `candidate-guardian.ts`, `ci-repair-loop.ts`, `gate-runner.ts`, `verification-engine.ts`, `verification.ts`, `verification-policy.ts`, `review-engine.ts`, `change-manifest.ts`, `git-checkpoint.ts`, `engineering-journal.ts`, `engineering-recovery.ts`, `merge-coordinator.ts`, `release-runner.ts`, `proposal-runner.ts`, `goal-acceptance.ts`, `final-acceptance-gate.ts`, `bootstrap-completion.ts`, `command-runner.ts`, `code-graph.ts`, `symbol-index.ts`, `world-model.ts`, `workspace.ts`, `microtask-runtime.ts`, `soak-runner.ts`, `owner-intervention-ledger.ts`, `native-tools.ts`, `ui-surface-discovery.ts`, `improvement-loop.ts`, `implementation-loop.ts`, `engineering-goal-loop.ts`, `repo-engineering-operations.ts`, `live-engineering-operations.ts`, `repo-inspector.ts`, `semantic-slice.ts`, `change-points.ts`, `finding-scope.ts`, `dev-handoff.ts`, `deferred.ts`, `cached-repo-scan.ts`, `atomic-file.ts`, `acceptance-session.ts`, …); `electron/self-engineering/` (3); `electron/bootstrap/{engineering,engineering-surface-ipc}.ts`; 20 `src/shared/*` files. 74 files total.
- **Owner module:** `engineering` — `engineering.yaml`, kind feature, provides `engineering.service@1` + `engineering.learning@1`, `requires: []`, `optional: []`, `state: []`, `permissions: []`, `health.critical: false`.
- **Inputs:** `EngineeringGoalContract`; repo + git state (`repo-inspector.ts:167 scanRepo`, `world-model.ts:374 buildWorldModel`); injected coder/reviewer text; IPC payload on `boss:engineering-goal-run`.
- **Outputs:** `EngineeringLoopSummary`/`EngineeringGoalSnapshot`; durable `engineering-loop.json` + `engineering-recovery.json`; git checkpoints; `artifacts/acceptance/*`; `EvidenceLedgerFile`; `candidate-record.json`, `soak-record.json`, `final-acceptance.json`, `bootstrap-completion.json`, `owner-interventions.json`.
- **State ownership:** manifest declares `state: []`. Durable state is JSON beside the task-ledger root (`main-commander.ts:888`); acceptance artifacts under `artifacts/acceptance`; loop fields in `engineering-loop-store.ts:14`.
- **Permissions:** `permissions: []`. Child processes only through allow-listed `runAllowedCommand` = `test|typecheck|build|lint` (`command-runner.ts:7,49`). `credential-boundary/sanitized-environment.ts` appears only as a comment (`command-runner.ts:37`).
- **Dependencies:** declared `requires: []`, `optional: []`. Kernel seams: `../git/git-gateway`, `../process/process-gateway`, `../workspace/path-utils`, `../commander/durable-json`, `../fs-util`, `../cache/content-cache`, `../telemetry/telemetry-store`.
- **Consumers:** `electron/main.ts:882,1287`; `electron/commander/main-commander.ts:758 runEngineeringGoal`, `:873 engineeringGoalStatus`; `electron/commander/plan-runner.ts:19`; `electron/self-evolution/self-evolution-coordinator.ts:29–32,440–446`; `scripts/acceptance-seeded-engineering.cjs`; `scripts/dogfood-engineering.cjs`.
- **Failure domain:** the loop aborts/rolls back on checkpoint failure or driver exception (`engineering-recovery.ts:39,78`; `main-commander.ts:788–845`); non-converged terminals roll the tree back; `engineering-surface-ipc.ts:86` reports `DEGRADED` per channel; contract-level `engineering.service@1` health is `READY` only.
- **Tests:** `tests/unit/{engineering-goal-loop,engineering-recovery,engineering-journal-layout,engineering-session-isolation,bootstrap-engineering,verification-ladder,verification-ledger-restore,gate-runner,self-healing-battery,version-impact,capability-gap,world-model-builder,host-regression-sentinel,host-fault-lab}.test.ts`, `tests/unit/engineering/goal-acceptance.test.ts`; `tests/acceptance/{verification-engine,ci-repair-loop,capability-gap-loop,recovery-loop,review-loop,execution-plan,final-acceptance,bootstrap-completion,candidate-guardian,version-checkpoint,publish-release,soak,autonomous-evolution-*}.test.ts`. Suites with **no** test importing them by name: `engineering/merge-coordinator.ts`, `engineering/microtask-runtime.ts`, `electron/self-engineering/{diagnosis,correction-store,self-mod-sandbox}.ts`.
- **Acceptance gates:** `acceptance:verify`, `acceptance:review`, `acceptance:plan`, `acceptance:self-healing`, `acceptance:capability-gap`, `acceptance:candidate`, `acceptance:version-checkpoint`, `acceptance:publish`, `acceptance:ci-repair`, `acceptance:final`, `acceptance:soak`, `acceptance:bootstrap-completion`, `acceptance:owner-ledger`, `acceptance:evidence-integrity`, `test:seeded`, `acceptance:evolution-*`. **No `acceptance:engineering` script exists.**
- **Can be disabled?:** No switch observed; `health.critical: false` is parsed by `electron/platform/capability-manifest.ts:294`, but the boot modules are constructed unconditionally at `electron/main.ts:882,1287`.
- **Can be removed?:** No — 8+ production consumers (`main.ts`, `main-commander.ts`, `self-evolution-coordinator.ts`) and the `engineering.service@1`/`engineering.learning@1` provisions; `src/shared/evidence-ledger.ts` and 6 other `src/shared` engineering files are imported repo-wide. `electron/self-engineering/**` **is** unwired (no importer outside config/docs) and can be removed without touching production.
- **Can be replaced?:** Only piecewise. The `engineering-loop-driver.ts`/`engineering-loop-store.ts` contract is injected-operations shaped.
- **Direct lateral dependencies?:** yes, the broadest of the non-kernel capabilities — `engineering → promotion` (`self-evolution/mutation-guard` in `verification.ts:7`, `verification-engine.ts:24`, `recovery-engine.ts:20`, `workspace.ts:6`; plus `candidate-gate`, `autonomous-evolution-trust`, `autonomous-evolution-identity`, `trust-problems`, `self-modification`), `→ status` (`evidence-ledger`×7, `acceptance-evidence`, `acceptance-contracts`, `owner-intervention`, `final-acceptance`, `soak`, `bootstrap-audit`, `telemetry-store`), `→ security` (`secret-scan`, `github-machine`, `github-gateway`), `→ tasks`, `→ tenx`, `→ workspace`, `→ theme`, `→ knowledge`, `→ learning` (`bootstrap/engineering.ts:68`), `→ providers`, `→ persistence`.
- **Shared mutable state?:** yes — `engineering-loop.json`/`engineering-recovery.json` (two writers, one directory); the target working tree itself (`applyScopedChanges`/`rollbackToCheckpoint`); `artifacts/acceptance/*`; `candidate-record.json`, `soak-record.json`, `final-acceptance.json`, `bootstrap-completion.json`, `owner-interventions.json`; `<userData>/.boss/learning`.
- **Root-trust relevance?:** **Yes.** `electron/engineering/acceptance-session.ts`, `bootstrap-completion.ts` and `owner-intervention-ledger.ts` are three of the 22 declared Root Trust Surface paths (`trust-policy/root-trust-surface.json`). In addition `autonomous-evolution-runner.ts:192,209,250` uses `ROOT_TRUST_FILES`/`ROOT_TRUST_PREFIXES`/`ROOT_TRUST_CHANGE`; `autonomous-evolution-identity.ts:110–116` reads `trust-policy/requirement-retirements.json` and `trust-policy/capability-registry.json`; `autonomous-evolution-surface.ts:66` references `ROOT_TRUST_SURFACE`; `acceptance-session.ts`/`owner-intervention-ledger.ts` use `src/shared/trust-problems.ts`.

## 12. Research

- **Current implementation location:** `electron/research/**` (15 root files + `evidence/`, `literature/`, `manuscript/`, `runtime/`); `electron/ingestion/**` (8); `electron/protocols/**` (6); `electron/bootstrap/research{,-ipc,-owner-ipc,-run-ipc}.ts`; 17 `src/shared/research-*.ts`. 63 files total.
- **Owner module:** `research` — `research.yaml`, kind feature, provides `research.service@1` + `research.ledger@1`, **`requires: knowledge.store@1`**, `health.critical: false`.
- **Inputs:** `boss:research-start` researchQuestion|goal, workspace (canonicalised), reviewers[], autonomy, hypothesis, providerPolicy, budgets; frozen `ResearchProtocol`; `ResearchContract`/`ReviewRound`; provider automation; OpenAlex/Crossref fetch (`literature/host-retrieval.ts:146,152,175,180`).
- **Outputs:** durable ledger `.boss/research/<id>.json`; `protocol.json`; `evidence-graph.json` + `run-*.json`; `citations.json`; `manuscript/{paper.md,paper.tex,references.bib,figures}`; `audit/{compile.json,final-audit.json}`; export `<workspace>/Research/<topic>`.
- **State ownership:** declares **`research-runs`, `research-protocols`, `research-contracts`, `research-reviews`** (sole owner confirmed in `architecture-ownership.json:27–30,79–84`). No literal `research-runs` path string appears in any `.ts` — UNKNOWN as a path.
- **Permissions:** `permissions: []`; no permission check or IPC sender validation in the four bootstrap files; 9 channels exposed via `electron/preload.ts:15–23` (owner-contract/audit/review-round channels are not in preload).
- **Dependencies:** the declared required edge `→ knowledge.store@1` (declared but **no import observed** — zero `knowledge` imports in `electron/research`, `electron/ingestion`, `electron/protocols`, `src/shared/research-*`). Kernel: `persistence` `StateStore` + provider automation injected at `electron/main.ts:832–838`.
- **Consumers:** `electron/main.ts:839,1049,1072,1294`; renderer `src/renderer/main.tsx:337–430` via `window.boss.*`; `tenx`: `electron/commander/workbook-dispatch.ts:34–36`, `workbook-production.ts:25` (ingestion imports).
- **Failure domain:** executor fails closed (`bootstrap/research.ts:70,80,85`); an 11-check `READY` gate (`research-service.ts:99–175`); step cap `min(maxSteps, 500)`; provider `maxAttempts 8` / backoff 20000; ledger/store throw (`research-ledger.ts:70`; `protocol-manager.ts:29,38,65`).
- **Tests:** `tests/unit/{research-battery,research-contract,research-partial-failure,research-review,research-wait-policy,bootstrap-research,workbook-ingestion-native,workbook-ingestion-structured,workbook-registry-roles,workbook-pdf-console}.test.ts`; `tests/acceptance/{workbook-production-acceptance,knowledge-reuse}.test.ts`.
- **Acceptance gates:** `acceptance:workbook`, `acceptance:knowledge`, `acceptance:requirements`, `acceptance:plan`. **No dedicated research script** — `scripts/acceptance-research-offline.cjs` and `scripts/acceptance-research-audit.cjs` exist but are not wired to `package.json`.
- **Can be disabled?:** UNKNOWN — no flag; the boot module is pushed unconditionally at `electron/main.ts:839`. `health.critical: false` means absence degrades, never fails boot.
- **Can be removed?:** Yes by design — every non-critical capability can be deleted and the platform stays bootable (`tests/unit/platform/platform-health.test.ts:111–123`), with dependents reporting "knowledge degraded".
- **Can be replaced?:** Yes — executor and runtime are injected (`bootstrap/research.ts:109–110`); `ResearchStageExecutor` contract; an offline `DefaultLevelBExecutor` alternative; `ResearchSemanticProvider` seam.
- **Direct lateral dependencies?:** yes — `research → tenx` (`../commander/durable-json`, 9 sites); `→ engineering` (`../engineering/repo-inspector`); `→ workspace` (`../workspace/path-utils`); `→ tasks` (`src/shared/workbook`); `→ automation` (`bootstrap/research.ts` → `provider-automation`); `→ status`; `→ security` (`ingestion/ingest.ts` → `src/shared/secret-scan`); `→ runtime`; `→ persistence`.
- **Shared mutable state?:** yes — `.boss/research/**`, `.boss/research-protocols/**`, `.boss/research-contracts/**`, `.boss/research-reviews/**` (single-owner by declaration); **plus undeclared writes outside the declared namespaces into the user workspace**: `<workspace>/experiments/bench-*.js` (`research-conductor.ts:850–856`) and `<workspace>/Research/**`.
- **Root-trust relevance?:** None in source, indirect only: its `tests/acceptance/**` suites sit on the declared Root Trust Surface (`tests/acceptance/**` is one of the 22 paths).

## 13. Evidence

> **NOT a declared capability.** There is no `config/capabilities/evidence.yaml`. The surface is split across **three** declared owner modules.

- **Owner module:** three declared modules own this surface — `status` (engine, repro snapshot, shared evidence/acceptance contracts), `host-status` (host evidence inspector + acceptance hub), `research` (research evidence graph). There is no capability literally named "Evidence".
- **Current implementation location:** `status` — `electron/evidence-engine.ts`, `electron/repro-snapshot.ts`, `src/shared/{evidence-ledger,evidence-inspector,acceptance-evidence,acceptance-contracts,acceptance-hub,acceptance-record,final-acceptance}.ts`. `host-status` — `electron/host/evidence-inspector.ts`, `electron/host/acceptance-hub-runner.ts`. `research` — `electron/research/evidence/{evidence-graph,graph-claims,repro-audit,run-analysis}.ts`.
- **Inputs:** `BossTask` + `RawArtifact[]` + `CouncilSession`; `ReproSnapshotInput` (git state, config hash, lockfile); `EvidenceInput`; scan roots/budgets; acceptance catalog rows; `AcceptanceSession` + report + attestation; `PrimaryRunRecord`.
- **Outputs:** `EvidenceBundle` with decision `PASS`/`HOLD_FOR_REVIEW`; `ReproductionSnapshot`; `EvidenceLedgerFile` entries; `EvidenceInspection` JSON; `AcceptanceReport` + `AcceptanceRecord`; `GateAttestation`; `FinalAcceptance`; `EvidenceGraphFile` + `run-*.json`; `ReproducibilityAudit`; `ManuscriptClaimsDerivation`.
- **State ownership:** `status.yaml` declares `state: []`. Gate reports/attestations live at `artifacts/acceptance/**` — **written by engineering-owned `engineering/acceptance-session.ts:21–43`**. Host runs write `artifacts/host-acceptance/latest.json` + `latest-record.json` (`scripts/host-acceptance.cjs:178,183`). `research.yaml:12` declares `research-runs`; `EvidenceGraph` writes `<root>/evidence/<id>/`.
- **Permissions:** all three manifests declare `permissions: []`. `electron/host/evidence-inspector.ts` is read-only by construction (header :26–29, no write path) and its CLI repeats the claim (`scripts/host-evidence.cjs:26`); the hub spawns catalog argv through `electron/host/process-runner.ts`.
- **Dependencies:** kernel (`durable-json`, process gateway); `status → promotion` via `src/shared/trust-problems` (`acceptance-evidence.ts:37`); `research` declares `requires: knowledge.store@1`.
- **Consumers:** `status` surfaces — `main.ts:84,1325`, `commander/task-finalizer.ts:62`, `bootstrap/task-lifecycle-ipc.ts:2`, `commander/main-commander.ts:729`; `src/shared/evidence-ledger.ts` is consumed by 7 `electron/engineering/*` files plus `src/shared/verification.ts:42`; `final-acceptance.ts` by `engineering/final-acceptance-gate.ts:24`, `src/shared/bootstrap-audit.ts`, `src/shared/soak.ts`. `host-status` surfaces — `scripts/host-evidence.cjs`, `scripts/host-acceptance.cjs`, `electron/host/acceptance-catalog.ts:358`. `research` surfaces — `research-service.ts:9,16–18,73`, `runtime/run-recorder.ts`, `manuscript/manuscript-assembler.ts`, `research-conductor.ts`.
- **Failure domain:** `evidence-engine.ts:35` throws on artifact content-hash mismatch (fail closed); the hub isolates per-check failure and always writes a report; the inspector reports truncation as `degraded`; `acceptance-evidence.ts:350` returns `TrustProblem[]` rather than booleans; `evidence-graph.ts:150` throws `Invalid evidence graph` on a corrupt schema.
- **Tests:** `tests/acceptance/{evidence-integrity,final-acceptance,soak,bootstrap-completion,owner-intervention-ledger,desktop-black-box-contract}.test.ts`; `tests/unit/{evidence-ledger,final-acceptance-gate-evidence,host-evidence-inspector,host-acceptance-hub}.test.ts`; `tests/unit/git-gateway.test.ts:73`. **No test imports `buildEvidenceBundle`; no test covers `electron/research/evidence/**`.**
- **Acceptance gates:** `acceptance:evidence-integrity`, `acceptance:final`, `acceptance:soak`, `acceptance:bootstrap-completion`, `acceptance:owner-ledger`, `acceptance:desktop-contract`, `acceptance:attest`, plus `acceptance:evolution-{identity,trust,adversarial}` which import `src/shared/acceptance-contracts.ts`. **No package.json script runs `scripts/host-acceptance.cjs` or `scripts/host-evidence.cjs`; `scripts/acceptance-research-audit.cjs` is also unwired.**
- **Can be disabled?:** `hostEvidenceInspector`/`hostAcceptanceHub` flags default `false` (`src/shared/host-maturity-flags.ts:35–43`), but **no consumer of `resolveHostFlags`/`hostCapabilityEnabled` exists in `electron/` or `scripts/`** — enforcement UNKNOWN. `status`/`research` have no disable switch; both are `health.critical: false`.
- **Can be removed?:** No — `src/shared/evidence-ledger.ts` and `final-acceptance.ts` are load-bearing for engineering, and `acceptance-contracts.ts` is imported by `src/shared/autonomous-evolution-trust.ts`. `electron/host/**` and `electron/research/evidence/**` each have no in-process importer outside their own module and scripts, so each is removable in isolation if its script is dropped.
- **Can be replaced?:** Piecewise — `acceptance-record.ts` has a single consumer (`scripts/host-acceptance.cjs:109`); `electron/evidence-engine.ts` and `repro-snapshot.ts` have no test, so each is replaceable behind its existing signature; `evidence-ledger.ts`'s `EvidenceEntry` fields are a contract for 7 engineering readers.
- **Direct lateral dependencies?:** yes — `host-status → status` (`host/evidence-inspector.ts:5–22`, `acceptance-hub-runner.ts:1–6`); `status → promotion` (`src/shared/trust-problems`); `research → knowledge` (declared); the reverse `engineering → status` edge is heavily used.
- **Shared mutable state?:** yes — `artifacts/acceptance/**` (`session.json`, `history/`, `attestations/`, gate reports, `owner-interventions.json`) is written by engineering-owned writers and read by status/host-status auditors; `artifacts/host-acceptance/latest.json` + `latest-record.json`; the research evidence root `<root>/evidence/<id>/`.
- **Root-trust relevance?:** **Yes**, for the `status` surfaces. `src/shared/acceptance-contracts.ts:76–381` declares per-gate `required_ids`/`out_of_scope_ids`/`report_file` — i.e. the **required-check declaration** — and is imported by `src/shared/autonomous-evolution-trust.ts` and `electron/engineering/autonomous-evolution-runner.ts`. `src/shared/acceptance-evidence.ts:350 verifyGateAttestation` checks session/commit/tree sha, required-id hash and attestation digest using `src/shared/trust-problems.ts` (promotion-owned). No file in `electron/host/**` or `electron/research/evidence/**` references `trust-policy/`, `credential-boundary/`, `promotion-gate/` or trust-epoch semantics.

## 14. Knowledge

- **Current implementation location:** `electron/knowledge/{knowledge-base,knowledge-foundation,knowledge-governance-store,knowledge-space-store,knowledge-store}.ts`; `electron/bootstrap/knowledge.ts`; 14 `src/shared/{knowledge,knowledge-claim,knowledge-extraction,knowledge-governance,knowledge-object,knowledge-retrieval-guard,knowledge-space,knowledge-staleness,learned-concept,learning-episode,archive-policy,data-lifecycle,data-retention,workbook-knowledge}.ts`. 20 files.
- **Owner module:** `knowledge` — `knowledge.yaml`, kind feature, provides `knowledge.store@1` + `theme.registry@1`, **`requires: persistence.store@1`**, `health.critical: false`.
- **Inputs:** `KnowledgeCandidate`/`KnowledgeClaim`; `extractKnowledgeFromDispatch(WorkBookDispatchRecord)`; `KnowledgeEntry`; `StateStore` snapshot for planner resources; `appPath` scan (world model / UI surface discovery).
- **Outputs:** `.boss/knowledge-base.json` (objects/quarantine/conflicts/gate_log); `.boss/world-model/`; `.boss/ui-surfaces.json`; `.boss/theme-registry.json` + `.boss/themes/`; a bounded prompt section (`main.ts:862–874`). **No IPC channel at all.**
- **State ownership:** declares **`knowledge-base`, `world-model`, `ui-surfaces`, `theme-registry`, `theme-packages`** (owners confirmed in `architecture-ownership.json`). The literal `theme-packages` path string is absent from all `.ts` — UNKNOWN as a path.
- **Permissions:** none declared; the module exposes no IPC/channel; no permission check anywhere in the 20 files.
- **Dependencies:** the declared required edge `→ persistence.store@1` (kernel `StateStore` injected at `main.ts:854–858`).
- **Consumers:** `electron/main.ts:854–874` (boot + `contextManager.setKnowledgeSectionProvider`), `main.ts:1121`; `electron/engineering/improvement-loop.ts:234`; `electron/theme/theme-knowledge.ts:16–17`; `src/shared/capability-gap.ts:20`.
- **Failure domain:** a corrupt base becomes empty in memory with `loadFailure` reported, nothing written until the next commit; `sectionForTask` catches to `""`; UI discovery failure yields a locked `defaultSurfaceContracts` + a "registry rejected" warning; tier/schema throws; claim validation returns all problems.
- **Tests:** `tests/unit/{knowledge-object,knowledge-foundation,knowledge-phase-f,bootstrap-knowledge,workbook-secrets-knowledge}.test.ts`, `tests/unit/knowledge/knowledge-lifecycle.test.ts`; `tests/acceptance/{knowledge-reuse,data-lifecycle-report,capability-gap-loop,theme-engine}.test.ts`.
- **Acceptance gates:** `acceptance:knowledge`, `acceptance:theme`, `acceptance:capability-gap`, `acceptance:desktop-workbook` (asserts `.boss/knowledge-base.json`), `data:lifecycle-report`.
- **Can be disabled?:** UNKNOWN — no flag; the boot module is pushed unconditionally at `electron/main.ts:860`. `health.critical: false`.
- **Can be removed?:** Yes by design — non-critical, the platform stays bootable without it, and dependents degrade (research reports "knowledge degraded").
- **Can be replaced?:** Partially — `KnowledgeFoundation` takes an injectable `base: KnowledgeBase`, but `KnowledgeStore`, `KnowledgeGovernanceStore`, `KnowledgeSpaceStore`/`DeferredSyncQueue` and all of `workbook-knowledge.ts` have **no production construction site** (tests only). UNKNOWN which backend is intended to replace `KnowledgeBase`.
- **Direct lateral dependencies?:** yes — `knowledge → tenx` (`../commander/durable-json` in 4 files; `src/shared/tenx/knowledge` types), `→ theme` (`bootstrap/knowledge.ts:10` → `theme-service`), `→ engineering` (`world-model`, `ui-surface-discovery`), `→ workspace` (`path-utils`), `→ tasks` (`workbook`, `workbook-dispatch`), `→ security` (`secret-scan`), `→ persistence`, `→ providers`, `→ runtime`, `→ status`.
- **Shared mutable state?:** yes — the declared files above, **plus a second writer of the `KnowledgeBase` class from engineering**: `electron/engineering/improvement-loop.ts:234` with root `artifacts/acceptance/knowledge-base.json` (`:116`).
- **Root-trust relevance?:** No. One touchpoint: `src/shared/data-retention.ts:124` lists `"trust-policy"` inside `PROTECTED_MARKERS`, so retention can never delete a trust-policy record.

## 15. Fleet

> **NOT a declared capability.** Declared owner of all Fleet paths is **`tenx`**; `electron/node/**` is owned by declared module **`node`** (`node.yaml`, provides `node.capability@1`).

- **Owner module:** `tenx` for the fleet/registry surface; `node` for `electron/node/**`. There is no capability literally named "Fleet".
- **Current implementation location:** `electron/fleet/federation-coordinator.ts`; `electron/tenx/{fleet-controller,network-registry,node-identity-registry,task-lease-registry,session-lifecycle}.ts`; `src/shared/fleet.ts`; `src/shared/tenx/{fleet,network,node,session}.ts`; `src/shared/{node-capabilities,autonomy-supervisor,microtask,resource-model}.ts`; `electron/node/{node-capability-registry,node-inspector}.ts`.
- **Inputs:** `FederationCoordinator.join/heartbeat/enqueue(FleetTask{taskId,requiredCapabilities,checkpoint,replaySafe})/checkpoint/mark/reassignAfterDropout`; `TenxFleetController.lease(taskId, ownerNode, {takeoverAllowed, replaySafety})`; `NodeCapabilityRegistry.refresh(nodeId, NodeProbeData)`; `inspectDevice()` (reads `node:os`/`process`); `TenantNodeRegistry.refresh(NodeRefreshFacts)`; `TenxNetworkRegistry.update(nodeId, routes)`; `TenxSessionLifecycle.open(provider, {taskKind, longLivedContextRequired})`; pure `JobHeartbeat`+`StallBounds`, `ResourceDemand`+`PhysicalBudget`.
- **Outputs:** `FleetNode`/`FleetAssignment`; `refreshStates()` → `{nodes, dropped}`; `TaskLeaseState` (`QUEUED`…`FAILED`) + `inventory()`; `NodeState` (`UNINITIALIZED`…`RECOVERING`) + `CapabilityVerdict[]`; `NodeProbeData`; `NodeCapabilityAdvertisement`; `Observation { WORKING|SLOW|STALLED|FAILED }` + `RecoveryLadderStep` R0–R8 + `StragglerDecision`; `Microtask[]`; `{allowedWorkers, reason}`.
- **State ownership:** **none declared** — `tenx.yaml state: []` and `node.yaml state: []`. Each store writes a caller-supplied JSON path (schemaVersion 1 envelopes) via `../commander/durable-json`. The **`node-registry`** namespace is owned by `persistence` (`persistence.yaml:28`; constructed at `electron/bootstrap/persistence.ts:143`). **`electron/host/host-observer-collector.ts:485–486` writes undeclared `boss/tenx/nodes.json` and `boss/tenx/fleet.json`.**
- **Permissions:** `tenx.yaml permissions: []`, `node.yaml permissions: []`. Only ambient OS reads (`node:os`, `node:fs`, `node:path`, `node:crypto`). No credential-boundary access observed.
- **Dependencies:** kernel-mediated `../commander/durable-json`; `electron/bootstrap/persistence.ts` for the node-registry store; constructors supplied by `electron/host/{host-observer-collector,fault-lab}.ts`.
- **Consumers:** `FederationCoordinator` ← `electron/host/host-observer-collector.ts:228`, `host/fault-lab.ts:274,485`, `src/shared/fault-lab.ts:183`; `NodeCapabilityRegistry` ← `electron/main.ts:88,703,1039`, `persistence.ts:143`, `doctor.ts`, `host-observer-collector.ts:200,208`; `inspectDevice` ← `host-status-ipc.ts:112`, `doctor.ts:132`; `Tenx*` registries ← `electron/tenx/observability.ts`, `host-observer-collector.ts:485–488`; `microtask` ← `electron/engineering/microtask-runtime.ts`, `main-commander.ts:313–337`; `resource-model` ← `electron/commander/degraded-controller.ts:5`; `autonomy-supervisor` — **tests only, no production consumer**.
- **Failure domain:** per-record isolation (one node's `FAILED`/`OFFLINE` never mutates another's record); a corrupt file fails closed by throwing on restore; dropout of replay-unsafe work without a checkpoint fails honestly rather than blind re-running; `host-observer-collector` guards each dimension to `UNAVAILABLE` without failing the report.
- **Tests:** `tests/unit/{fleet,fleet-two-node,node-capabilities,standalone-node,autonomy-supervisor,phase-j-scenarios}.test.ts`, `tests/unit/tenx-phase-10{b,c,d,e,l,n,p,r}.test.ts`.
- **Acceptance gates:** `architecture:ratchet`, `architecture:snapshot`, `architecture:ownership`, `acceptance:architecture`, `test`, `typecheck`. `verify:certificate` has **no** runtime-intelligence reference. **`economics:*` do NOT gate Fleet.**
- **Can be disabled?:** No switch exists. `tenx.yaml`/`node.yaml` declare `bootModules: []`, so nothing boots them; there is no config toggle. The fleet coordinators have no production call site outside host observers, so they are already dormant in production.
- **Can be removed?:** `electron/fleet/**` + the listed `electron/tenx/*` and `src/shared/{fleet,tenx/*}.ts` are referenced only by host observers, `electron/tenx/observability.ts` and tests → removable with observer edits. `electron/node/**` is **not** removable alone (wired at `persistence.ts:143`, consumed at `main.ts:703,1039`, `host-status-ipc.ts:112`).
- **Can be replaced?:** Yes at the pure-contract seam — `src/shared/fleet.ts` and `src/shared/tenx/*` are platform-neutral (enforced by `electron/tenx/platform-audit.ts` + `tests/unit/tenx-phase-10q.test.ts`). `electron/tenx/host-adapter.ts` is documented as the port but is a **non-exported interface with zero implementations or importers** — prose only.
- **Direct lateral dependencies?:** yes — `node → tenx` (`electron/node/node-inspector.ts:2`, `node-capability-registry.ts:4` import `src/shared/node-capabilities.ts`); `tenx → tasks` (`src/shared/microtask.ts:1` → `./task-ir`); `tenx → security` (type-only: `src/shared/tenx/{node,scheduler}.ts` → `../github-machine`). All are **undeclared** in the 3-edge graph.
- **Shared mutable state?:** yes — each store is one JSON file with whole-file atomic write (no locking); two writers on one path last-write-win. The one-writer rule is enforced only for declared namespaces; the tenx stores write undeclared paths.
- **Root-trust relevance?:** No. No assigned path matches the protected-surface globs and none imports `trust-policy/`, `electron/credential-boundary/` or `electron/promotion-gate/`.

## 16. GitHub

> **NOT a declared capability.** Owner modules: `security` (`electron/github/**`, `src/shared/github-url.ts`), `providers` (`electron/input/github-resolver.ts`), `promotion` (`electron/promotion-gate/github-promotion-adapter.ts`).

- **Owner module:** `security` (`electron/github/**`, `src/shared/github-url.ts`); `providers` (`electron/input/github-resolver.ts`); `promotion` (`electron/promotion-gate/github-promotion-adapter.ts`). There is no capability literally named "GitHub".
- **Current implementation location:** `electron/github/{bootstrap,github-app-auth,github-config,github-gateway,github-guardian-policy,github-machine-runtime,live-acceptance,machine-identity-layout,node-github-self-check,secret-provider}.ts`; `src/shared/github-url.ts`; `electron/input/github-resolver.ts`; `electron/promotion-gate/github-promotion-adapter.ts`.
- **Inputs:** `--pem-file/--app-id/--installation-id/--repositories/--boss-data-dir` (`bootstrap.ts:13–37`); `github-machine-identity.json`; `GitHubHttpRequest`; github.com URLs.
- **Outputs:** `GitHubResult<T>` ok / `GitHubErrorCode`; `GitHubAuditRecord`; `GitHubNodeSelfCheck`; `github-machine-live-acceptance.json`; `GithubResolveResult.checkoutDir`.
- **State ownership:** `.boss/github-machine-identity.json`, `.boss/secret-vault.json`, `.boss/node-machine-identity.json` (`machine-identity-layout.ts:29–36`); in-memory install-token cache; repo cache `<cacheRoot>/<githubCacheKey>/` + `.codex-boss-github.json` marker.
- **Permissions:** `src/shared/permission.ts` kinds `filesystem|repo|network|secret|side-effect`, `manifestAllows`/`manifestNarrow`, `SecurityClass PUBLIC|INTERNAL|SECRET|GUARDIAN`, `desktopMutationGate` fails closed on `computer:<action>`; the guardian policy maps `ALLOW|ROOT_OWNER_REQUIRED|DENY` over 26 `GitHubOperation`s. **No `capability/permission-contract` grant is used by the gateway.**
- **Dependencies:** `src/shared/{github-machine,github-url,secret-scan}`; `electron/security/secret-vault-store`; `electron/process/process-gateway`; `electron/git/git-gateway`; `electron/fs-util`; `electron/credential-boundary/github-credential-provider`.
- **Consumers:** `electron/bootstrap/providers.ts:7`, `electron/bootstrap/persistence.ts:137` (GithubResolver); `electron/engineering/{release-runner.ts:31,ci-repair-loop.ts:19}`; `electron/self-evolution/{self-evolution-host.ts:157,self-evolution-coordinator.ts:9}`; `src/shared/tenx/{scheduler.ts:10,node.ts:13}`; `scripts/acceptance-github-machine-identity.cjs`.
- **Failure domain:** 10 `GitHubErrorCode`s classified from HTTP; retries bounded `maxRetries <= 3`, exponential ≤ 30 s; the key ceremony fails closed when `safeStorage.isEncryptionAvailable()` is false; the resolver throws on git failure and reports `reusedCache` on a marker match.
- **Tests:** `tests/unit/github-machine-identity.test.ts`, `tests/unit/machine-identity-layout.test.ts` (PF-DEBT-008 writer/reader label parity), `tests/acceptance/publish-release.test.ts`, `scripts/acceptance-github-machine-identity.cjs` (mock E2E). **No catalogue suite covers `github-resolver.ts` or `github-promotion-adapter.ts` directly — UNKNOWN.**
- **Acceptance gates:** `acceptance:github-machine`, `acceptance:github-machine:live` (`electron dist-electron/electron/github/live-acceptance.js`), `bootstrap:github-machine`, `security:scan`. CI runs `acceptance:github-machine` (`.github/workflows/ci.yml`).
- **Can be disabled?:** Yes — `GitHubMachineIdentityConfig.enabled`; a missing config file returns `{configured:false}` (`github-machine-runtime.ts:50`); the shipped example (`.codex-boss/config/github-machine-identity.example.json`) sets `"enabled": false`.
- **Can be removed?:** Refactorable, not deletable by the autonomous path — owned by `security`, and the adapter is a hard consumer for remote promotion.
- **Can be replaced?:** Partly — `SecretProvider`, `GitHubHttpTransport`, `GitHubTransport` and `gitExec` are injectable. The boot/runtime composition roots and `machine-identity-layout.ts` are the fixed seam.
- **Direct lateral dependencies?:** yes — `runtime → security` (`electron/main.ts` → `github/github-machine-runtime.ts`), `providers → security` (`bootstrap/providers.ts:7`), `tenx → security` (`src/shared/tenx/{scheduler,node}.ts`), `engineering → security` (`ci-repair-loop.ts:19`), `promotion → security` (`github-promotion-adapter.ts`).
- **Shared mutable state?:** yes — the `.boss` secret vault is shared with Machine Identity; the repo cache directory under `cacheRoot` is shared across resolves.
- **Root-trust relevance?:** **Yes, indirect.** `electron/promotion-gate/` and `electron/credential-boundary/` are ROOT_TRUST/OWNER_AUTHORITY protected (`src/shared/root-authority/protected-surface.ts:89–101`; `.github/CODEOWNERS`). `github-promotion-adapter.ts:3` reads `REQUIRED_PROMOTION_CHECKS` — the **required-check declaration** (`src/shared/promotion-checks.ts:35`: `["quality","unit","acceptance","package"]`). No `trust-policy/` read, no epoch write.

## 17. Machine Identity

> **NOT a declared capability.** Every path is owned by declared module **`security`**.

- **Owner module:** `security` — all eight paths below are owned by it. There is no capability literally named "Machine Identity".
- **Current implementation location:** `src/shared/github-machine.ts`; `electron/github/machine-identity-layout.ts`; `electron/github/github-app-auth.ts`; `electron/credential-boundary/{credential-boundary,github-credential-provider,sanitized-environment}.ts`; `src/shared/secret-vault.ts`; `electron/security/secret-vault-store.ts`.
- **Inputs:** `validateGitHubMachineIdentityConfig` input object; PEM private key + app/installation id + repository allowlist at the ceremony; injected `protect`/`unprotect` platform crypto; host `process.env` for sanitization.
- **Outputs:** `GitHubMachineIdentityConfig`; `GitHubMachineIdentityLayout` files + `vaultLabel`; `SecretLease` with `dispose()`; `AmbientCredentialObservation`/`CredentialBoundaryAssessment`; `BossCredentialResult AVAILABLE|BLOCKED_EXTERNAL`.
- **State ownership:** config JSON, `secret-vault.json` (ciphertext-only; vault label `"machine-identity"`), `node-machine-identity.json`; installation-token cache. The credential boundary holds no disk state.
- **Permissions:** `security.permission@1` is the declared provision. Secret purpose taxonomy `api-key|oauth-token|signing-key|backup-passphrase|adapter-credential`; `guardianAreaFor` maps signing-key → `production.signing`, backup-passphrase → `backup.deletion`; **rotation of any existing secret and every guardian-area mutation requires a `guardToken`**.
- **Dependencies:** `src/shared/{github-machine,secret-vault,secret-scan,guardian}`; `electron/security/secret-vault-store`; `electron/credential-boundary/*`; `electron/runtime-paths`; `electron/process/process-gateway`; `node:crypto` (RS256 JWT); Electron `safeStorage`/`dialog`.
- **Consumers:** `electron/bootstrap/providers.ts:7`; `machine-identity-layout` used by both `electron/github/bootstrap.ts:42` (writer) and `github-machine-runtime.ts:48` (reader); `github-credential-provider` by `self-evolution-host.ts:4,156`, `self-evolution-coordinator.ts:7`, `github-promotion-adapter.ts:2`; `sanitized-environment` by `electron/root-authority/execution-profile.ts:5`; `github-machine` types by `src/shared/tenx/{scheduler,node}.ts`.
- **Failure domain:** the config validator returns `null` → fail closed; `validateSecretVault` rejects any `plaintext` field and a vaultName mismatch; a missing credential is `AUTH_MISSING`/`BLOCKED_EXTERNAL` and **never falls back to the Owner**; an unreadable vault throws rather than loading empty.
- **Tests:** `tests/unit/github-machine-identity.test.ts`, `tests/unit/machine-identity-layout.test.ts`, `tests/unit/credential-boundary.test.ts`, `tests/unit/workbook-secrets-knowledge.test.ts`, `tests/acceptance/capability-escape.test.ts`.
- **Acceptance gates:** `bootstrap:github-machine`, `acceptance:github-machine`, `acceptance:github-machine:live` (real credential self-check then branch→commit→push→PR), `security:scan`.
- **Can be disabled?:** Yes for the integration (`enabled:false` / absent config ⇒ GitHub only); the boundary modules themselves are always-on host code. The private-key ceremony is a one-time Root Owner dialog action.
- **Can be removed?:** No — `security` owns every path and declares `provides: security.permission@1`; `electron/credential-boundary/` is ROOT_TRUST/OWNER_AUTHORITY protected.
- **Can be replaced?:** Partly — crypto backend, `SecretProvider`, HTTP transport and the credential provider are injected seams; the layout declaration and config schema are single-source-of-truth contracts.
- **Direct lateral dependencies?:** `promotion → security` observed — `electron/root-authority/execution-profile.ts:4–5` imports `credential-boundary` + `sanitized-environment`. Machine identity itself is intra-`security`.
- **Shared mutable state?:** yes — `secret-vault.json` under the `.boss` root is the one shared store (writer ceremony and reader runtime must agree on file name + label); process-wide `process.env` is read but never mutated.
- **Root-trust relevance?:** **Yes (highest tier).** `electron/credential-boundary/` is in `ROOT_PROTECTED_MANIFEST` and `OWNER_AUTHORITY_PATHS` (`src/shared/root-authority/protected-surface.ts:89–93`); `credential.boundary.mutate` is `REQUIRE_OWNER` (`contracts.ts:79`) and maps to the `protectedSurfaceMutation` policy knob (`root-policy.ts:108`); CODEOWNERS protects the directory. No `trust-policy/` file is read or written here.

## 18. Self Evolution

> **NOT a declared capability.** Declared owner module: **`promotion`** (`promotion.yaml`, kind feature, provides `promotion.gate@1`).

- **Owner module:** `promotion` (`config/capabilities/promotion.yaml`, kind feature, provides `promotion.gate@1`). There is no capability literally named "Self Evolution".
- **Current implementation location:** `electron/self-evolution/{self-evolution-coordinator,self-evolution-host,host-operations,mutation-context,mutation-guard,self-target-resolver,stable-runtime-pointer}.ts` + `sandbox/{sandbox-backend,sandbox-capability,sandbox-drive,toolchain-materialization,windows-appcontainer-backend}.ts` + `sandbox/windows-appcontainer/{launcher.cs,launcher-source.ts}`; `electron/promotion-gate/{exact-sha-gate,github-promotion-adapter,promotion-controller}.ts`; `electron/stable-candidate/{candidate-supervisor,runtime-isolation,workspace-manager}.ts`; wired at `electron/bootstrap/engineering.ts:3,57` and `electron/main.ts:914–917`.
- **Inputs:** `SelfEvolutionTaskRequest { taskId, objective, workspace, goal?, runId?, maxIterations? }`; `EvolutionRunContext { runId, candidateRoot, candidateWorkspace, stableRoot, baseSha, ledgerFile, evidenceDirectory }`; host options `stableRoot/evolutionRoot/governanceRoot/appPath/userData/rootOwner/worker/sandbox/credentialProvider/transport`.
- **Outputs:** `SelfEvolutionRunReport` with `outcome ∈ NOT_SELF|EMERGENCY_STOPPED|BLOCKED_EXTERNAL|WAITING_FOR_ROOT_OWNER|PROMOTED|REJECTED|ROLLED_BACK|CANDIDATE_FAILED`; host operation results; `runs/<runId>.json`, `runs/<runId>-promotion.json`, `runs/<runId>-boot.json`.
- **State ownership:** `governanceRoot` — `evolution-control.json`, `evolution-frozen.sentinel`, `emergency-evidence.jsonl`; `root-audit-ledger.jsonl`; `stable-pointer.json`. In-memory only: `EvolutionRunRegistry`/`evolutionRuns`, mutation-guard config + caches. Candidate worktree/branch `evolution/<runId>`. **None of this is a declared durable namespace.**
- **Permissions:** `RootOperation` vocabulary via `RootAuthority.classify`; `EVOLUTION_WORKER_ALLOWED`/`_DENIED` + `classifyEvolutionCommand` deny shells, git writes, `gh`, curl/wget, registry tools; `assertMutationAllowed` throws `SelfMutationDeniedError`; no guardian tokens in any self-evolution file.
- **Dependencies:** `promotion-gate/*`; `root-authority/*`; `stable-candidate/*`; `emergency-control/*`; `credential-boundary/*`; `src/shared/root-authority/{promotion-state,contracts}`; `electron/engineering/*`; `electron/git/git-gateway`.
- **Consumers:** `electron/bootstrap/engineering.ts:3,57` composes it; `electron/main.ts:914–917` installs it on `MainCommander`; `electron/commander/main-commander.ts:262–277` is the **mandatory self-target route with no fallback**; `electron/engineering/{recovery-engine,workspace,verification,verification-engine}.ts` call `assertMutationAllowed`.
- **Failure domain:** every Candidate failure is contained (`CandidateSupervisor.supervise()` never throws on Candidate failure); `emergencyStop` freezes the kill switch and aborts the candidate; promotion only from `PROMOTABLE` or a SHA-approved `WAITING_FOR_ROOT_OWNER`; an unrecordable Root decision makes `classify` throw.
- **Tests:** `tests/unit/{self-evolution-route,self-evolution-production-defaults,self-target-resolver,evolution-quiescence,evolution-sandbox,promotion-gate,candidate-gate,stable-candidate,stable-candidate-fault-isolation,self-elevation,root-authority,root-authority-red-team,root-trust-authority-lockdown}.test.ts`, `tests/unit/sandbox-*.test.ts`, `tests/acceptance/autonomous-evolution-{identity,trust,independent,adversarial}.test.ts`, `tests/unit/root-trust-authority-lockdown.test.ts`.
- **Acceptance gates:** `acceptance:autonomous-evolution`, `acceptance:evolution-battery` (`-- --rounds 20` in CI), `verify:certificate`, `acceptance:evolution-identity`, `acceptance:evolution-trust`, `acceptance:evolution-independent`, `acceptance:evolution-adversarial`, `acceptance:evolution-independent`, `acceptance:autonomous-evolution`.
- **Can be disabled?:** **Yes** — `EvolutionKillSwitch` latching freeze via `evolution-control.json` or the external `evolution-frozen.sentinel`, re-read from disk on every check with no cache, so restart cannot clear a freeze; it gates candidate creation, promotion and process spawn. Ordinary Stable chat/work/research never consult it.
- **Can be removed?:** No — `promotion` owns the directory; `src/shared/autonomous-evolution-trust.ts:137` classifies it `EVOLUTION_ENGINE`; `MainCommander` routes self-target tasks to it with no fallback.
- **Can be replaced?:** Refactorable internally (worker, sandbox, transport, registry, host handlers are injected); `self-evolution-coordinator.ts:48–74` names itself the ONLY production composition root.
- **Direct lateral dependencies?:** yes — `promotion → engineering` (`self-evolution-coordinator.ts` → `engineering/engineering-loop-driver`, `engineering-loop-store`, `src/shared/engineering-loop`; `promotion-controller.ts` → `engineering/owner-intervention-ledger`), `promotion → security` (coordinator → `credential-boundary`; `self-evolution/host-operations.ts` and `promotion-controller.ts` → `root-authority`, `emergency-control`), `promotion → status`, `promotion → tasks`, `promotion → tenx`, `promotion → workspace`, `promotion → persistence`, `promotion → runtime`. Reverse: `engineering → promotion`, `learning → promotion` (via `behaviour-epoch` contract only), `security → promotion` (root-authority contracts), `providers → promotion` (`provider-intelligence` → `behaviour-epoch`), `status → promotion` (`self-cognition` → trust classifier).
- **Shared mutable state?:** yes — process-global `evolutionRuns` registry and mutation-guard module config (deliberately in-memory only); `governanceRoot` files are the one durable shared surface, held outside every candidate workspace.
- **Root-trust relevance?:** **Yes (highest tier).** `gate.self.rewrite`, `stable.worktree.write`, `stable.runtime.write`, `self.elevation` are DENY floors; `RootAuthority` requires the audit ledger to live outside the candidate; `src/shared/root-authority/**` is OWNER_AUTHORITY-protected; `src/shared/autonomous-evolution-trust.ts` is the generator of `trust-policy/root-trust-surface.json`; `trust-epoch-finalization.yml` is the only place an epoch advances. No self-evolution module reads or writes `trust-policy/` directly.

## 19. Runtime Intelligence

> **NOT a declared capability.** Declared owner module: **`tenx`**. (`src/shared/provider-intelligence.ts` and `provider-outcome.ts` are owned by `providers`.)

- **Owner module:** `tenx`. `src/shared/provider-intelligence.ts` and `provider-outcome.ts` are owned by `providers`, not `tenx`. There is no capability literally named "Runtime Intelligence".
- **Current implementation location:** `electron/runtime-intelligence/{intelligence-store,live-capture,node-profiler,node-telemetry-log,outcome-source,prospective-store,replay-cases,replay-corpus-io,runtime-intelligence-service}.ts`; `src/shared/runtime-intelligence/` (27 files).
- **Inputs:** `collectNodeSnapshot(NodeProfilerOptions)` with injectable probes; `service.recordOutcome` / `ingestOutcome` / `ingestRealOutcomes({dataRoot})`; `RuntimeIntelligenceCapture` receives a `TaskLedgerRecord` **after** `super.save()` plus `WORKER_COMPLETED`/`WORKER_FAILED` bus events; `readRealOutcomes` reads `<dataRoot>/.boss/telemetry.json` + `.boss/learning/episodes.jsonl`; pure `TaskProfile`/`ContinuationSignals`/`ContextRecord`/`ReplayCorpus`.
- **Outputs:** `NodeCapabilitySnapshot` where every fact is `Measurement<T>` (`MEASURED|UNKNOWN|UNREADABLE|UNAVAILABLE|NOT_MEASURED`); `ModelCapabilityRecord`; `RuntimeObservation`; `SchedulingRecommendation` with `authority: ADVISORY_ONLY`, `productionRoutingAuthority: false`, `qualificationHostSelection: false`; `ContinuationAssessment`; `ContextLifecyclePlan`; `EvaluationReport` with `grantsExecutionAuthority: false`; plus durable `observations.jsonl`, `recommendations.jsonl`, `models.json`, `nodes.json`, `skill-telemetry.jsonl`, `context-records.json`, `nodes-sampled.jsonl`, `nodes-archive.jsonl`, `nodes-index.json`.
- **State ownership:** **none declared.** `runtimeIntelligenceRoot(dataRoot) = <dataRoot>/.boss/runtime-intelligence` is fixed in code. Store reads degrade to empty + `degradedReason` instead of throwing.
- **Permissions:** no declared surface (`tenx.yaml permissions: []`). `node-profiler.ts:110–115` reads provider-key **names** only (values never read) plus `PATH`; `node:net` TCP probe. No credential-boundary usage. `replay-corpus-io.ts` applies `src/shared/secret-scan` (`redactSecrets`/`scanSecrets`) plus a `REDACTED_FIELDS` whitelist before export.
- **Dependencies:** kernel-mediated `electron/commander/durable-json`, `electron/commander/task-ledger`, `electron/commander/event-bus`, `electron/runtime-paths`, `src/shared/hash`. Node builtins `fs/path/os/net`; **no `node:child_process`** (explicitly avoided).
- **Consumers:** production is exactly two seams — `electron/bootstrap/persistence.ts:24,126–131` (`createCaptureObservingLedger` wraps the one `TaskLedger` write path) and `electron/bootstrap/automation.ts:13,85–89` (attaches the capture as a 4th `DomainEventBus` observer). `scripts/runtime-intelligence-report.cjs` and `-diff-guard.cjs` load the compiled modules; `scripts/package-installer.cjs:87–105` **requires the built bundle to contain `executionAuthority: false` and `grantsExecutionAuthority: false`**. 29 suites under `tests/unit/runtime-intelligence/`. **Not wired into `electron/main.ts` by design.**
- **Failure domain:** fail-open by construction — every capture method catches everything and marks `captureHealthy=false` without propagating; every profiler probe is isolated so a throwing probe costs one metric, and **absence is never rendered as 0 or `[]`**; the plane holds no executive authority. A corrupt plane file cannot stop the work it observes.
- **Tests:** all 29 `tests/unit/runtime-intelligence/*.test.ts` (`service`, `live-capture`, `intelligence-store`, `node-profiler`, `node-profile`, `node-telemetry-log`, `prospective-window`, `replay-cases`, `replay-corpus`, `replay-corpus-io`, `outcome-ingestion`, `evaluation-report`, `scheduler-benchmark`, `continuation-benchmark`, `continuation-evaluator`, `scheduling-advisor`, `skill-loadout`, `skill-replay`, `calibration`, `context-lifecycle`, `contracts`, `measurement`, `model-ledger`, `policy-freeze`, `snapshot-retention`, `step-completion`, `telemetry`, `dispatch-attribution`, `authority-boundary`).
- **Acceptance gates:** `test`, `typecheck`, and **`package:installer`** (fails closed if the shipped bundle stops declaring `executionAuthority:false` / `grantsExecutionAuthority:false`). `economics:*` and `verify:certificate` do **not** gate this plane. `scripts/runtime-intelligence-report.cjs` and `-diff-guard.cjs` have **no package.json script** — they are not wired gates.
- **Can be disabled?:** No config switch. Not a boot module; the capture is constructed unconditionally at `persistence.ts:126` and attached unconditionally at `automation.ts:88`. Disabling means editing a kernel bootstrap file.
- **Can be removed?:** Largely yes, but not by config: removal requires un-wiring `persistence.ts:24,126–131` (+ the `capture` service field) and `automation.ts:13,85–89`. No declared state namespace has to be reclaimed.
- **Can be replaced?:** Yes. Any replacement must keep the contract `schemaVersion 1` + `Measurement` union, the policy registry hashes/literals, the temporal-guard input/target split, and the literal `authority:"ADVISORY_ONLY"` / `productionRoutingAuthority:false` / `qualificationHostSelection:false` / `grantsExecutionAuthority:false` strings.
- **Direct lateral dependencies?:** yes — `tenx → status` (`electron/runtime-intelligence/outcome-source.ts:26` → `../telemetry/telemetry-store`); `tenx → learning` (`:25` → `../learning/episode-store`); `tenx → security` (`replay-corpus-io.ts:23` → `src/shared/secret-scan`). None appears in `architecture-graph.json`'s 3 edges.
- **Shared mutable state?:** yes — plane files live under an **undeclared** path, so the one-authoritative-writer registry does not cover them; two writers on one root last-write-win (atomic rename, no lock). `ProspectiveWindowStore` caches the parsed log keyed on size+mtime, so an external writer is picked up on the next read. Capture is read-only over the ledger.
- **Root-trust relevance?:** No — and this is **test-enforced**. `tests/unit/runtime-intelligence/authority-boundary.test.ts` walks every `.ts` in both directories and asserts `assessProtectedPaths` has no hits/escapes, `ProtectedSurfaceGuard.assessChangeSet` = `ALLOW` with `codeownersMissing === false`, `deriveChangeClass` = `ORDINARY_AUTONOMOUS_CHANGE`, and `classifySurface` = `PRODUCT_SURFACE`, with positive controls proving `trust-policy/trust-epoch.json`, `src/shared/autonomous-evolution-trust.ts`, `.github/CODEOWNERS`, `electron/root-authority/root-authority.ts`, `electron/promotion-gate/*`, plan-stage gates and a `"../outside"` escape are all caught / `REQUIRE_OWNER` / `DENY`. The plane only *reports* boundary facts it is handed.

## 20. Adaptive Learning

> **NOT a declared capability.** Declared owner module: **`learning`** (`learning.yaml`, kind feature, provides `learning.episode@1`, **`state: []`**).

- **Owner module:** `learning` (`config/capabilities/learning.yaml`, kind feature, provides `learning.episode@1`). There is no capability literally named "Adaptive Learning".
- **Current implementation location:** `electron/learning/` (23 `.ts`: `learning-service`, `episode-store`, `adaptive-flag-store`, `outcome-evaluator`, `task-fingerprint`, `routing/{adaptive-scorer,exploration-policy,routing-feedback}`, `concepts/{concept-registry,concept-miner,concept-merge,concept-split}`, `providers/{behaviour-model,behaviour-epoch,change-point-detector,model-observer,model-snapshot,provider-profile,version-resolver}`, `evolution/{historical-replay,policy-candidate,promotion-gate,shadow-evaluator}`) + `electron/experience/{experience-store,experience-recorder}.ts`.
- **Owner-module split (real):** `electron/learning/**` → `learning`; `src/shared/adaptive-routing.ts`, `provider-intelligence.ts`, `provider-profile.ts` → `providers`; `src/shared/adaptive-flags.ts`, `host-maturity-flags.ts`, `task-fingerprint.ts` → `tasks`; `src/shared/learning-episode.ts`, `learned-concept.ts` → `knowledge`; `electron/experience/**` + `src/shared/experience.ts` → declared module `experience`.
- **Inputs:** `EpisodeStore.append(EpisodeAppendInput)` over `LearningEpisode`; `TaskFingerprinter.build(FingerprintInput)`; `ModelObserver.observe(RawModelObservation)`; `AdaptiveFlags`. **`ITelemetry`/`state` is NOT read by learning.**
- **Outputs:** `LearningEpisode` JSONL rows + `SemanticEvaluationRevision` rows; `ProviderBehaviourProfile`; `LearningConcept`; `BehaviourEpoch`; `ModelSnapshot`; `AdaptiveRoutingDecision` + `RoutingFeedbackRecord`; `AdaptivePolicy`/`PolicyCandidate`; `ProviderIntelligencePanel` + `EpisodeDrilldown`; `LearningControlState`.
- **State ownership:** manifest declares **`state: []`**. In fact `LearningService` writes under `<userData>/.boss/learning` (`electron/bootstrap/engineering.ts:68`): `episodes.jsonl`, `revisions.jsonl`, `adaptive-flags.json`, `model-snapshots.json`, `behaviour-epochs.json`, `provider-profiles.json`, `concepts.json`, `routing-feedback.json`, `policy-gate.json`. `ExperienceStore` writes `.boss/experience.json` (`persistence.ts:161`).
- **Permissions:** no manifest permissions. All five adaptive flags default **FALSE** (`DEFAULT_ADAPTIVE_FLAGS`); `resolveAdaptiveFlags`/`resolveHostFlags` enable only on an explicit boolean `true`; a corrupt/missing file ⇒ all-off + `degradedReason`. Owner controls over IPC `boss:learning-control`, `boss:provider-intelligence`, `boss:learning-episode`.
- **Dependencies:** `src/shared/{provider-intelligence,adaptive-flags,adaptive-routing,provider-profile,provider-outcome,learning-episode,task-fingerprint,learned-concept,model-identity,behaviour-epoch}.ts`; kernel `electron/commander/durable-json`; `yaml`/`fs`. **No trust-policy, credential-boundary, promotion-gate or root-trust import anywhere under `electron/learning/`.**
- **Consumers:** `electron/bootstrap/engineering.ts` (creates `LearningService`); `electron/main.ts:89,134,1041`; `electron/bootstrap/host-status-ipc.ts:130–163`; `electron/host/host-observer-collector.ts:12,338–380,463`; `electron/runtime-intelligence/outcome-source.ts:25,152`; `electron/host/fault-lab.ts:12,382,415`; `electron/experience/experience-recorder.ts` wired at `electron/bootstrap/automation.ts:83`.
- **Failure domain:** declared for the facade — "every method is failure-isolated: a learning-layer exception never propagates into task execution"; every store catches and records `degradedReason` instead of throwing; `panel()` falls back to an empty panel; `AdaptiveScorer.rerank` returns `undefined` on any exception (A27). **`RoleRouter` is built with NO adaptive reranker**, so `applyAdaptive` returns the deterministic order and the scorer is wired into no production route.
- **Tests:** `tests/unit/engine-phase-0..11.test.ts`, `tests/unit/engine-acceptance.test.ts`, `tests/unit/experience-capability.test.ts`. Catalogue: phase-8 covers `{learning,promotion}`; phases 0–7 and 9–11 plus `engine-acceptance` cover `learning`.
- **Acceptance gates:** `typecheck`, `test`, `build`. **No `acceptance:*` script names learning.** No learning/experience suite is `alwaysRun`, but `tests/unit/platform/{architecture-ratchet,capability-manifest,state-ownership}.test.ts` (always-run) police the ownership/state declarations the module feeds.
- **Can be disabled?:** **Yes, at two levels** — `adaptiveProviderLearning` off disables learning entirely while keeping cached profiles (IPC `set-learning`); `adaptiveRouting` off keeps learning but restores the deterministic `RoleRouter` order. `adaptiveCapabilityEnabled` requires both the umbrella and the capability's own flag.
- **Can be removed?:** Practically yes for the capability (own kind, no declared state, `critical: false`), but removal breaks `electron/bootstrap/engineering.ts`, the `main.ts` learning wrapper, `host-status-ipc.ts`, `host-observer-collector.ts`, `runtime-intelligence/outcome-source.ts`, `host/fault-lab.ts` and 13 engine-phase suites. `electron/experience/` is separable.
- **Can be replaced?:** Yes behind two seams — `AdaptiveReranker` (`src/shared/adaptive-routing.ts:86–88`, consumed by `RoleRouter.applyAdaptive`) and `SemanticVectorStore`/`EmbeddingBackend`. The episode JSONL is the declared source of truth, so derived stores are rebuildable/discardable (`rebuildDerived`/`resetDerived`).
- **Direct lateral dependencies?:** `learning → promotion` is **candidate but refuted** — `electron/learning/evolution/promotion-gate.ts` imports only `./policy-candidate`, `./historical-replay`, `./shadow-evaluator`; it never imports `electron/promotion-gate/` or `src/shared/promotion-checks.ts`. `learning → providers` only via shared `providers`-owned contract files. Real edge observed: **`learning → knowledge`** (`src/shared/learning-episode.ts` is `knowledge`-owned), and **`status → learning`** (`host-status-ipc.ts:133,137,145`; `host/fault-lab.ts:12`).
- **Shared mutable state?:** yes — the `.boss/learning` files are touched by more than one capability: `electron/runtime-intelligence/outcome-source.ts` (tenx/status) reads `episodes.jsonl` while learning writes it; `electron/host/fault-lab.ts` reads the same file. No lock or ownership check between readers and writers.
- **Root-trust relevance?:** No. Its own `evolution/promotion-gate.ts` promotes a *routing policy version*, not a Boss build, and `learning.yaml` `requires` is empty.

## 21. Self Cognition

> **NOT a declared capability.** Declared owner module: **`status`**.

- **Owner module:** `status` (`config/capabilities/status.yaml`, kind feature, provides `status.snapshot@1`). There is no capability literally named "Self Cognition".
- **Current implementation location:** `electron/self-cognition/facts.ts` (read-only host adapter) and `src/shared/self-cognition/{contracts,anatomy,describe,drift}.ts` (pure).
- **Inputs:** `SelfFactOptions { repositoryRoot }`; read from disk — `config/capabilities/*.yaml` (parsed with `yaml`), `config/capability-modules.json`, `config/architecture-baseline.json`, `electron/main.ts` text, `package.json` scripts; a source walk of `electron/` + `src/shared/`; `classifySurface`, `assessProtectedPaths`, `ProtectedSurfaceGuard`.
- **Outputs:** `SelfFacts`; `BossSelfModel` (components/capabilities/dataFlows/unreadable/`authority:{canDescribeSelf:true, canDiagnose:false, canMutateSelf:false}`); `SelfDescription`; `Availability<…>` answers from `describeComponent`/`describeCapability`/`authorityOf`/`dependencyPath`/`affectedBy`; `SelfModelDriftReport` + `selfModelHash` (sha256 over anatomy); graphs `componentGraph`/`capabilityGraph`/`dependencyGraph`/`dataFlowGraph`.
- **State ownership:** `state: []`; the module **writes nothing** — `tests/unit/self-cognition/boundary.test.ts:169–174` asserts the pure modules contain no `node:fs` import and none of `writeFileSync/appendFileSync/rmSync/unlinkSync/mkdirSync/renameSync`; `facts.ts` calls only `readFileSync`/`readdirSync`/`existsSync`.
- **Permissions:** `status.yaml permissions: []`. `authorityOf` is explicitly a report, not a grant ("it has no way to grant permission … `CAN_DESCRIBE_SELF != CAN_AUTHORIZE_SELF`"); the verdict object is exactly `authority|ownerReview|paths`.
- **Dependencies:** `yaml`, `node:fs`, `node:path`; `src/shared/autonomous-evolution-trust.ts`, `src/shared/root-authority/protected-surface.ts` (both `promotion`), `electron/root-authority/protected-surface-guard.ts` (`security`); `src/shared/hash.ts` (`persistence`).
- **Consumers:** **no production consumer** — `electron/main.ts` has zero references to `self-cognition`; no bootstrap module imports it. Actual callers are the CLI scripts `scripts/self-view.cjs:62–65`, `scripts/self-case-record.cjs:106–109`, `scripts/self-diagnosis.cjs:56–57`, plus four unit suites.
- **Failure domain:** an unreadable fact is recorded in `unreadable` — "never skipped: a manifest that failed to parse must not look like a capability that does not exist"; an unreadable source propagates as an `UNKNOWN`/`NOT_MEASURED` `Availability` rather than a default; a missing boot-module reference is `NOT_MEASURED`, not "wired".
- **Tests:** `tests/unit/self-cognition/{self-model,describe,drift,boundary}.test.ts` (all catalogued under `status`; `boundary.test.ts` under `{security,status}`).
- **Acceptance gates:** `typecheck`, `test`; `tests/unit/platform/{test-impact,architecture-ratchet,state-ownership}.test.ts` (always-run) validate the facts it reads. `scripts/self-view.cjs` has **no package.json script** — it is not a gate. **NOT_FOUND** for a capability-specific `acceptance:*` script.
- **Can be disabled?:** Nothing to switch off in the app — the module is never instantiated at runtime.
- **Can be removed?:** Yes, mechanically (kind: feature, `critical: false`, `state: []`, zero production importers). Removal costs `scripts/{self-view,self-case-record,self-diagnosis}.cjs` and the four suites. It is also the only module that reads `config/architecture-baseline.json` into the self model.
- **Can be replaced?:** Yes — `buildSelfModel(facts)` is a pure function of the `SelfFacts` bundle, so any fact collector can substitute.
- **Direct lateral dependencies?:** yes — `status → promotion` (`facts.ts:17` → `classifySurface` from `src/shared/autonomous-evolution-trust.ts`), `status → promotion` (`facts.ts:18` → `assessProtectedPaths` from `src/shared/root-authority/`), `status → security` (`facts.ts:19` → `ProtectedSurfaceGuard`), `status → persistence` (`hash.ts`). No `status → learning` edge from this submodule.
- **Shared mutable state?:** none — every store is read-only; `SelfFacts`/`BossSelfModel` are passed by value.
- **Root-trust relevance?:** **Yes, directly.** It consumes the repository's own trust classifier and owner-review guard rather than a private rule, and reports `ROOT_TRUST_SURFACE`/`EVOLUTION_ENGINE`/`VERIFICATION_SURFACE`/`PRODUCT_SURFACE` plus `ALLOW`/`REQUIRE_OWNER`/`DENY`. `tests/unit/self-cognition/boundary.test.ts` asserts the facts are the real repository's, that authority verdicts are the repository's own (`trust-policy/trust-epoch.json` ⇒ ROOT_TRUST_SURFACE, `tests/acceptance/bootstrap-completion.test.ts` ⇒ ROOT_TRUST_SURFACE, `scripts/acceptance-verify.cjs` ⇒ VERIFICATION_SURFACE, `electron/main.ts` ⇒ PRODUCT_SURFACE, `package.json` ⇒ PRODUCT_SURFACE + `REQUIRE_OWNER`), and that the module cannot diagnose/mutate/authorize (code, comments stripped, must not contain `symptom|hypothesis|treatment|repair|remediate`, `\bheal\b`, `diagnose(`, or a `diagnos`-named export; must contain `canDiagnose: false` and `canMutateSelf: false`; must hold no `node:fs` and no write call; must export no `grant|apply|mutate|override|authorize`; `describeSelf` JSON must not contain `unhealthy|degraded|root cause|failure mode detected`).

## 22. Self Diagnosis

> **NOT a declared capability.** The split in `config/capability-modules.json` is real: `src/shared/self-diagnosis.ts` is a **file** (76 lines, failure clustering + RFC draft) owned by **`engineering`**, while `src/shared/self-diagnosis/` is a **directory** (`engine`, `hypotheses`, `observations`, `plan`, `policy`, `treatment`) owned by **`status`**, together with `electron/self-diagnosis/sources.ts`.

- **Owner module:** two declared modules — `status` (the directory `src/shared/self-diagnosis/**` and `electron/self-diagnosis/sources.ts`) and `engineering` (the file `src/shared/self-diagnosis.ts`, `electron/self-engineering/diagnosis.ts`, `src/shared/self-healing-battery.ts`). There is no capability literally named "Self Diagnosis".
- **Current implementation location:** `status` — `src/shared/self-diagnosis/**`, `electron/self-diagnosis/sources.ts`. `engineering` — `src/shared/self-diagnosis.ts`, `electron/self-engineering/diagnosis.ts`, `src/shared/self-healing-battery.ts`.
- **Inputs:** `diagnose({ model: BossSelfModel, observations?, sources?, at, priors?, unattributableFrom? })`; `sources.ts` reads `<dataRoot>/.boss/telemetry.json`, `<dataRoot>/.boss/recovery.json`, `<dataRoot>/state.json`. Thresholds are explicit: `RUNTIME_FAILURE_SHARE_LIMIT = 0.3`, `RECOVERY_COUNT_LIMIT = 5`. Engineering side: `clusterFailures(TelemetryLike[])` over FAILED telemetry records + `RfcCorrection[]`.
- **Outputs:** `DiagnosisReport` (symptoms, ranked hypotheses, `spread`, plan, treatment proposals, `unreadable`, `sourceFailures`, `authority:{canDiagnose:true, canProposeTreatment:true, canExecuteTreatment:false, mutatesAnatomy:false}`, `engineVersion`, `policyHash`); `DiagnosticPlan` (`COLLECT|INSPECT|COMPARE|REPRODUCE`); `TreatmentProposal` with an `executable: false` literal; `diagnosisPolicyHash()`. Engineering side: `{clusters: FailureCluster[], topRfc?: RfcDraft}`; `HealingPlan` + `detectScenario`/`stagnated`.
- **State ownership:** `state: []` on `status`. The status-side engine and its sources **write nothing** — "These are adapters, not owners … none of them writes, repairs, retries or changes what it observed". `SelfObservationSource` has one method `observe` and "no `write`, no `apply` and no handle to the thing it observes". `self-healing-battery.ts` is pure and writes nothing.
- **Permissions:** `status.yaml permissions: []`. The status-side module claims no authority to act: `executable: false` on every proposal; `OWNER_ONLY_AUTHORITIES = ["ROOT_TRUST_SURFACE","EVOLUTION_ENGINE"]` plus `ownerReview === "REQUIRE_OWNER"` force `REQUEST_OWNER_REVIEW` / `requiredAuthority: "OWNER"`. An unknown component authority also yields no automated treatment.
- **Dependencies:** `src/shared/self-cognition/contracts.ts` (`BossSelfModel`), `describe.ts#affectedBy` for blast radius, `src/shared/hash.ts` for the policy hash, `node:fs`/`node:path` in `sources.ts` only. Engineering path: `src/shared/correction.ts` + `electron/telemetry/telemetry-store.ts`.
- **Consumers:** status-side — **no production consumer** (`electron/main.ts` and every `electron/bootstrap/*.ts` have zero `self-diagnosis` references). Real callers: `scripts/self-diagnosis.cjs:56–59`, `scripts/self-case-record.cjs` (`--from-diagnosis`), plus the unit suites. Engineering-side — `electron/self-engineering/diagnosis.ts#diagnoseTelemetry` has **NO consumer anywhere in the repository** (grep for `diagnoseTelemetry` returns only its own definition); `src/shared/self-diagnosis.ts` is reachable only through it. **The engineering half is an orphan.**
- **Failure domain:** a source that throws is isolated and recorded in `sourceFailures`; an unreadable file becomes an `unreadable` entry with its reason, never a silent zero; a missing measurement is `UNKNOWN`/`NOT_MEASURED`, and the engine notes that "no reading that was taken was unhealthy, which is not the same as the system being healthy"; a reading about an unknown component is kept and flagged, not dropped; a report with no symptoms is a complete answer.
- **Tests:** `tests/unit/self-diagnosis/{diagnosis,observations}.test.ts` (catalogued under `status`); `tests/unit/self-healing-battery.test.ts` (catalogued under `engineering`). **No test file imports `src/shared/self-diagnosis.ts` or `electron/self-engineering/diagnosis.ts`.**
- **Acceptance gates:** `typecheck`, `test`; `acceptance:self-healing` exists for the engineering self-healing battery. **NOT_FOUND** for a capability-specific gate for the status-side diagnosis engine; no covering suite is `alwaysRun`.
- **Can be disabled?:** Nothing to switch off at runtime — no flag, no boot module, not wired; it runs only when a CLI or unit caller invokes `diagnose`. `src/shared/host-maturity-flags.ts` contains no self-diagnosis flag.
- **Can be removed?:** The status-side directory is removable from the app's perspective (feature, `critical: false`, `state: []`, no production importer) **but** its types are the vocabulary of `src/shared/self-case-record/**` (`case.ts:14–16`, `timeline.ts:39–41`, `recurrence.ts:20–21`, `dogfood.ts:19`), so removal breaks Self Case Record, `scripts/self-diagnosis.cjs` and `scripts/self-case-record.cjs`. The engineering file is an orphan and can go once `self-engineering/diagnosis.ts` goes with it.
- **Can be replaced?:** Yes at the `SelfObservationSource` seam — sources are handed in and asked once, so another source set replaces `telemetrySource`/`recoverySource`/`taskOutcomeSource` without changing the engine. A replacement engine must keep the `engineVersion`/`policyHash` pairing to stay auditable.
- **Direct lateral dependencies?:** `status → engineering` — the two halves of this capability are split across owner modules, and `electron/self-engineering/diagnosis.ts:2–3` imports `src/shared/self-diagnosis` (the only production import of that file). `engineering → providers` — `src/shared/self-healing-battery.ts:15–16` imports `buildRepairPlan` from `src/shared/computer-recovery.ts` (`providers`-owned). `status → promotion` by value, not by import (`AuthorityClass` values are produced by the promotion-owned trust classifier). No `status → learning` edge.
- **Shared mutable state?:** none. Diagnoses are pure functions of observations and the passed self model; the only durable interaction is read-only. `self-healing-battery.ts` takes `grants: ReadonlySet<string>` and returns a verdict.
- **Root-trust relevance?:** Yes on the status side: `OWNER_ONLY_AUTHORITIES` names `ROOT_TRUST_SURFACE` and `EVOLUTION_ENGINE`, and `proposeTreatments` refuses to propose an automated treatment across that boundary. The engineering battery's gates are build gates (`typecheck|unit|build|review-pass|restart-smoke|…`), not trust-policy gates. No path in either half reads `trust-policy/`, `credential-boundary/`, `promotion-gate/` or declares a required check.

## 23. Self Case Record

> **NOT a declared capability.** Declared owner module: **`status`**.

- **Owner module:** `status`. There is no capability literally named "Self Case Record".
- **Current implementation location:** `electron/self-case-record/case-store.ts` (durable append-only JSONL store) and `src/shared/self-case-record/{case,timeline,recurrence,dogfood}.ts` (pure).
- **Inputs:** `CaseStore.openCase({caseId, at?, trigger, incidentClass, provenance, affectedComponents?, symptoms?, relatedTasks?, relatedCommits?, relatedRuntimeEvents?})`; `append({caseId, type, at?, detail})`; `appendEvent(timeline, {at, type, detail})`; the `diagnose` output is copied in by callers; `eventsByCase` for protocol-violation detection.
- **Outputs:** `CaseTimeline` and the folded `SelfDiagnosisCase` (status, firstPass, diagnosesConsidered, validations, treatmentProposals, treatmentActuallyPerformed, recurrenceLinks, provenance); `TimelineOperation { ok, timeline?, case?, problems }`; `RecurrenceLink`; `PriorEvidence[]`; `LessonCandidate[]`; `DogfoodMetrics`; `SelfDiagnosisPolicyDefectReport[]`; `CaseStoreStatus`.
- **State ownership:** `state: []` in `status.yaml`. The store writes exactly one file, **`case-record.jsonl`**, under `CaseStoreOptions.rootDir`, by `fs.appendFileSync` only — one row per **event**, never a rewritten snapshot; a bad row is counted in `unreadableRows`, not silently dropped. `scripts/self-case-record.cjs:35` defaults the root to `runtime-data/.boss/self-case-record`.
- **Permissions:** `status.yaml permissions: []`. The plane records but cannot act: `TREATMENT_SOURCES = ["OWNER","HNS","EXTERNAL_SYSTEM"]` and "`SELF_DIAGNOSIS` is not in this vocabulary and never will be"; `appendEvent` refuses a `TREATMENT_PERFORMED` whose `performedBy` is not one of those.
- **Dependencies:** `src/shared/self-case-record/*`; `src/shared/self-diagnosis/{hypotheses,plan,treatment,policy}.ts` (types + `HIGH_CONFIDENCE_THRESHOLD`); `src/shared/hash.ts` indirectly through the diagnosis policy hash; `node:fs`/`node:path` only in `case-store.ts`.
- **Consumers:** **no production consumer** — `electron/main.ts` has zero `self-case-record`/`CaseStore` references and no bootstrap module imports it. Real callers: `scripts/self-case-record.cjs:90–93` (`--list`, `--case`, `--candidates`, `--from-diagnosis`, `--resolve`, `--validate`) plus three unit suites. It is the intended store for the status-side diagnosis engine, but nothing calls `CaseStore.openCase` from a diagnosis run.
- **Failure domain:** a torn/unparsable row is counted in `unreadableRows` and skipped, so "a damaged log reports itself instead of quietly shortening a history"; an unreadable file yields `degradedReason` and no records; `foldCase` returns `problems` instead of throwing and refuses a timeline whose sequence is not its order; `openCase` refuses a duplicate case id, a blank trigger, an unparsable instant and missing/blank provenance; `appendEvent` refuses a backwards timestamp, a second `CASE_RESOLVED`, an unknown event type and an unknown validation verdict.
- **Tests:** `tests/unit/self-case-record/{case,dogfood,boundary}.test.ts` (all catalogued under `status`).
- **Acceptance gates:** `typecheck`, `test`. **NOT_FOUND** for a capability-specific `acceptance:*` script; no covering suite is `alwaysRun`. `scripts/self-case-record.cjs` has no package.json entry, so it is not a gate.
- **Can be disabled?:** Nothing to disable at runtime — no flag, no boot module, no IPC surface, no production call site.
- **Can be removed?:** Yes in principle (feature, `critical: false`, `state: []`, no production importer), but it is the declared persistence for case history and its own record identity: removal costs `scripts/self-case-record.cjs`, the three suites, the `LESSON_CANDIDATE`/prior-evidence boundary that gates knowledge promotion, and the dogfood metrics. It depends on `src/shared/self-diagnosis/**` types, so it cannot outlive that directory.
- **Can be replaced?:** Yes — the seam is the event vocabulary plus `foldCase` ("a store that loses state can be rebuilt from the events alone"; "the fold is a pure function of the events"), so any storage that preserves one row per event and the sequence reproduces the same case. `CaseStoreStatus` (`schemaVersion`, counts, `unreadableRows`, `bytes`) is what a replacement must report.
- **Direct lateral dependencies?:** intra-`status` only — `electron/self-case-record` → `src/shared/self-case-record` → `src/shared/self-diagnosis/*` are all `status`-owned. One `status → persistence` edge through `src/shared/hash.ts`. No `→ learning`, `→ promotion` or `→ providers` import observed.
- **Shared mutable state?:** one durable file, `case-record.jsonl`, written by `CaseStore.appendRow` and read by `timelines()`/`records()`/`status()`; no other capability writes it; every read re-parses the whole log, so no in-memory object is shared or mutated (`boundary.test.ts:117–128` asserts folding does not mutate the timeline).
- **Root-trust relevance?:** No direct `trust-policy/`/`credential-boundary/`/`promotion-gate/`/trust-epoch/required-check interaction. Relevance is by **boundary discipline**, asserted in `tests/unit/self-case-record/boundary.test.ts`: (a) the pure modules (`case.ts`, `timeline.ts`, `recurrence.ts`) must not import `node:fs` nor contain `writeFileSync|appendFileSync|rmSync|unlinkSync|mkdirSync|renameSync|child_process|execSync|spawn`, and must export no `execute|perform|diagnose|repair`-named symbol nor call `diagnose(`; (b) the module "does not know the self model exists" — neither the pure modules nor `case-store.ts` may contain the string `self-cognition`; (c) the store prototype chain must expose no `update|delete|remove|rewrite|replace|reset|clear|truncate|execute|apply|treat|repair|diagnose` and must expose `openCase|append|records|record|timelines|status|lessonCandidates|priorEvidence`; (d) appending leaves the first row byte-identical (`afterAll.startsWith(afterFirst)`); (e) `priorEvidenceOf` returns evidence whose keys are exactly `caseId|closedAt|componentId|failureMode|finalDisposition` with no `measurement`, so a past case cannot be mistaken for a current reading.

## 24. UI

> **NOT a declared capability.** `src/renderer/**` is in the `exempt` list of `config/capability-modules.json`, quoted verbatim: *"the renderer is exercised by the desktop black-box contract, which launches the real application; that suite is always-run, so a renderer change cannot be under-tested by being unowned"*. The theme/UI-surface files map to declared module **`theme`** (`theme.yaml`, kind feature, provides `theme.ipc@1`, **requires `theme.registry@1`** whose durable owner is `knowledge`).

- **Owner module:** **exempt** — `src/renderer/**` has no owning capability by a recorded decision in `config/capability-modules.json`; the theme/UI-surface files are owned by declared module `theme`. There is no capability literally named "UI".
- **Current implementation location:** `src/renderer/{main.tsx,state.ts,theme.ts,theme-measure.ts,styles.css,global.d.ts}` + 12 components (`AttachmentTray`, `ConversationContextMenu`, `GoalRunPanel`, `HistoryNameDialog`, `HumanInterventionCard`, `ManagerPanel`, `OwnerSummary`, `ProviderIntelligence`, `ResearchProgress`, `ThemePanel`, `WorkspacePathField`); `electron/theme/{builtin-themes,theme-knowledge,theme-service,theme-storage,visual-capture}.ts`; `src/shared/{theme,theme-generation,theme-intent,theme-visual-check,ui-surface,ui-surface-ids}.ts`; `electron/bootstrap/{theme-ipc,settings-ipc}.ts`. 12 theme files.
- **Inputs:** the `window.boss` bridge (`src/renderer/global.d.ts`, `electron/preload.ts` `ipcRenderer.invoke`); `ThemeSnapshot`/`ThemePreviewView`; `AppSnapshot`.
- **Outputs:** React DOM into `#root`; an injected `<style>` element for the active theme and preview; IPC invocations over `boss:*` channels.
- **State ownership:** **none durable** — the renderer holds React state only. The active theme is owned by `ThemeService`/`ThemeStorage` under `electron/theme/`; `src/renderer/theme.ts` states "the main process owns validation and the activation decision; this state is only the applied result".
- **Permissions:** the renderer is context-isolated behind `contextBridge`; no fs/network permission declared; a theme `overrides.css` is written but "never executed" (`theme-storage.ts:10`).
- **Dependencies:** `theme.yaml requires: [theme.registry@1]` (durable owner `knowledge`). The renderer imports `../shared/*` only.
- **Consumers:** `electron/preload.ts` exposes the bridge; `tests/unit/renderer-workspace-path-ui.test.ts` reads renderer sources; `scripts/acceptance-workspace-paths.cjs` drives the shipped app; `scripts/acceptance-theme.cjs` runs `tests/acceptance/theme-engine.test.ts`.
- **Failure domain:** a renderer crash/absence cannot stop background work — `tests/unit/ui-isolation.test.ts` (R-902) asserts a task completes with "No publish callback, no UI, no renderer"; a theme read failure leaves the shipped appearance in place (`main.tsx:49`).
- **Tests:** `tests/acceptance/{theme-engine,desktop-black-box-contract}.test.ts`; `tests/unit/{theme-engine,theme-generator,ui-surface,ui-isolation,renderer-workspace-path-ui,bootstrap-ipc-modules}.test.ts`. Catalogue: `theme` ← `theme-engine`, `theme-generator`, `ui-surface`, `recovery-loop`, `bootstrap-{ipc-modules,knowledge}`. Note `desktop-black-box-contract.test.ts` is catalogued under engineering/promotion/status — **not** the renderer.
- **Acceptance gates:** `acceptance:theme`, `acceptance:workspace-paths`, `acceptance:desktop-contract`, `test:impact:verify`.
- **Can be disabled?:** Theme can be disabled — `theme-storage.ts:94–99` returns `undefined` for a malformed package and `src/shared/theme.ts planActivation` falls back to `builtin-dark`. The renderer cannot be disabled; `index.html` is the only UI.
- **Can be removed?:** No — `src/renderer/main.tsx` is the Vite entry (`vite.config.mjs`, `index.html`) and `electron/preload.ts` types the `BossBridge` the renderer requires.
- **Can be replaced?:** Yes in principle — the renderer is exempt/unowned, and the only contract is `BossBridge` in `src/shared/contracts.ts` plus the `UI_SURFACE_IDS` vocabulary.
- **Direct lateral dependencies?:** none observed — grep for `trust-policy|trust-epoch|credential-boundary|promotion-gate|root-trust|qualification` across `src/renderer/**` returns no matches.
- **Shared mutable state?:** yes — one React root and one injected `<style>` element per document; `window.localStorage` key `codex-boss:history-collapsed` (`main.tsx:89`); `UI_TOKEN_GROUPS`/`UI_SURFACE_IDS` are frozen module constants.
- **Root-trust relevance?:** No. No renderer or theme file appears in `ROOT_TRUST_SURFACE_PATHS`.

## 25. Persistence / Event Journal

- **Current implementation location:** `electron/state-core/{database,event-journal,event-bus-bridge,event-consumer,state-repository,transaction,migration-runner,namespace-migration,decision-ledger-migration,shadow-compare,state-quarantine,recovery,schema-version,platform-soak}.ts`; `electron/bootstrap/state-core.ts`; `electron/bootstrap/persistence.ts`; `electron/store.ts`; `electron/history-repository.ts`; `electron/evaluation/evaluation-store.ts`; `electron/cache/content-cache.ts`; `src/shared/{session-lifecycle,session-state,decision-ledger,config-layering,dsh-presets,evaluation,hash,policy,external-session}.ts`.
- **Owner module:** **two declared modules, both kernel** — `persistence` (`persistence.yaml`, critical, **19 namespaces**) and `state-core` (`state-core.yaml`, critical, provides `state.store@1`/`state.journal@1`/`state.migration@1`, owns `state-core:migration`).
- **Inputs:** data root (`app.getPath("userData")`), legacy JSON stores, `PersistenceCrypto` callbacks, migration step list, `DatabaseHandle`; `persistence.ts` takes `{ dataRoot, historyRoot, cacheRoot, appPath, crypto }`.
- **Outputs:** `DatabaseHandle`; `StateRepository`; `EventJournal`; `EventConsumer`; `JournalBridge`; `NamespaceMigrationRegistry`; `StateQuarantine`; `RecoveryReport`; `MigrationRunResult`; durable `.boss/state/state.db` + checkpoint `.db` files.
- **State ownership:** `state-core` owns `state-core:migration` only. **`decision-ledger` is deliberately NOT claimed there** — `persistence` remains its single authoritative owner, exactly as Phase 01 declared and as `tests/unit/bootstrap-persistence.test.ts` asserts (the manifest carries a long comment explaining that declaring it under state-core would be the second-owner failure the registry exists to catch). `persistence` owns 19 namespaces: `history, tasks, app-state, attachments, provider-capabilities, api-settings, session-lifecycle, node-registry, external-sessions, runtime-budget, interventions, decision-ledger, workspaces, workspace-selection, permission-manifest, project-state, experience, runtime-resources, task-contexts`.
- **Permissions:** none declared — both manifests have `permissions: []` and `surface: []`. `database.ts` needs fs + the `node:sqlite` builtin.
- **Dependencies:** `state-core.yaml requires: []`; `persistence.yaml requires: []`. Internal chain `database.ts` ← `transaction`/`state-repository`/`event-journal`; `migration-runner` ← `shadow-compare` ← `decision-ledger-migration`.
- **Consumers:** `electron/bootstrap/state-core.ts` (the only production entry), `electron/main.ts` (builds both boot modules), `electron/commander/decision-ledger-store.ts` (the legacy side). Real lateral in-degree: **15 capabilities import `persistence`** and 20 import `runtime`.
- **Failure domain:** `state-core.ts:167–184` catches everything and returns `DEGRADED` + the legacy JSON ledger; it refuses `schema-ahead` before writing; `database.ts:67–70` throws when `node:sqlite` is missing.
- **Tests:** `tests/unit/state-core/{state-core,event-bus-bridge,state-core-boot,decision-ledger-migration}.test.ts`; `tests/unit/platform/{durable-event-correctness,durable-event-real-host-scale,scale-synthetic,platform-soak,qualification-provenance}.test.ts`; `tests/acceptance/{state-core-crash,state-migration-report,platform-soak-report}.test.ts`; `tests/unit/bootstrap-persistence.test.ts`.
- **Acceptance gates:** `state:migration-report`, `state:probe`, `soak:platform`, `platform:certificate`, `test:platform-qualification`, `acceptance:desktop-contract`, `test:impact:verify`, `test:real-host-scale` (private control plane only).
- **Can be disabled?:** **Yes for the event core** — `state-core.ts` degrades to the JSON ledger on any open/integrity/schema failure (`active: false`). `persistence` has no disable path.
- **Can be removed?:** No — `persistence` is kernel-critical and `main.ts` calls `createPersistenceModule`/`createStateCoreModule` unconditionally; the state core itself is additive over the JSON stores.
- **Can be replaced?:** Only by another boot module implementing `BootModule<PersistenceService>` / `BootModule<StateCoreService>`; the `shadow-compare` + `promote()` path exists to replace the JSON *store*, not the module.
- **Direct lateral dependencies?:** yes, as the kernel — `persistence → {attachments, experience, identity, node, project, providers, runtime, security, status, tasks, tenx, workspace}` (12), and is imported by 15 capabilities. `state-core → {knowledge, persistence, providers, runtime, status, tenx}`. No `trust-policy`/credential/promotion import found.
- **Shared mutable state?:** yes — the single `DatabaseSync` handle (one-writer rule), `transactionDepth` WeakMap, `state_namespace.owner` uniqueness, `event_journal(producer, idempotency_key)` UNIQUE.
- **Root-trust relevance?:** No. Grep for `trust-policy|trust-epoch|credential-boundary|promotion-gate` across `electron/state-core/**`, `electron/bootstrap/{state-core,persistence}.ts`, `electron/store.ts`, `electron/history-repository.ts` returns no matches.

## 26. Qualification

> **NOT a declared capability.** There is no `config/capabilities/qualification.yaml`; `.github/workflows/**`, `vitest.*.mjs` and most `scripts/*.cjs` are absent from `config/capability-modules.json`. `config/architecture-baseline.json` is the ratchet input read by the declared `runtime` module via `architecture:ratchet`.

- **Owner module:** **undeclared** — no `config/capabilities/qualification.yaml` exists and none of these paths appears in any manifest. `config/architecture-baseline.json` is read by the declared `runtime` module through `architecture:ratchet`. There is no capability literally named "Qualification".
- **Current implementation location:** `vitest.qualification.config.mjs`, `vitest.tiers.mjs` (tier + prerequisite declarations), `.github/workflows/{ci,platform-qualification,trust-epoch-finalization}.yml`, `scripts/{qualification-corpus-provenance,platform-certificate,platform-soak,qualify-assertion-reader,qualify-external-repo,acceptance-prestart}.cjs`, `tests/unit/platform/qualification-provenance.test.ts`, `docs/root-trust-change-proposal-ci-qualification-pointer.md`, `docs/acceptance/`, `Update-Plan/Platform-Foundation/Phase-08-Production-Qualification-and-Promotion.md`, `config/architecture-baseline.json`.
- **Inputs:** generated Phase 01–04 artifacts under `artifacts/platform-foundation/{phase-01..04}/`; a real full-unit-tier pairing record (`verify:targeted-vs-full`); an accumulated host corpus over the four roots `.codex-boss`, `artifacts`, `runtime-data`, `history`; `trust-policy/trust-epoch.json`.
- **Outputs:** `artifacts/platform-foundation/phase-05/platform-certificate.json`; a schema-2 provenance record with `corpus.commitmentDigest` = sha256 over `root\0path\0size`; GITHUB_STEP_SUMMARY rows; a trust-epoch PR on branch `trust-epoch/boss-root-trust-<epoch>`.
- **State ownership:** none — no namespace, no durable state. The certificate declares `bypassesRootOrOwnerGate` as a non-configurable `false`. The only write the trust workflow performs is `trust-policy/trust-epoch.json`.
- **Permissions:** `ci.yml` and `platform-qualification.yml` are `contents: read`; `trust-epoch-finalization.yml` is `contents: write` + `pull-requests: write` **behind the `boss-root-trust-owner` environment with a required reviewer and `can_admins_bypass: FALSE`**.
- **Dependencies:** `vitest.qualification.config.mjs` imports `PLATFORM_QUALIFICATION_TEST_FILES` from `vitest.tiers.mjs`; the three qualification suites declare `requires: ["accumulated-host-corpus"|"phase-artifact"|"full-suite-record"]` and a named `producer`; `tests/unit/test-layers.test.ts` machine-checks that no push-CI tier declares those prerequisites.
- **Consumers:** `pnpm run test:platform-qualification`; `pnpm run platform:certificate`; `pnpm run soak:platform`; `tests/acceptance/{platform-certificate,platform-soak-report,data-lifecycle-report,targeted-vs-full}.test.ts`; the private control plane `zhiheng-zhang-Mera/Boss-Qualification-Control`.
- **Failure domain:** `platform-qualification.yml` exits 0 **by design** and reports `HOSTED_RUNNER_NOT_A_QUALIFICATION_HOST` / `BLOCKED_BY_REAL_SOAK_EVIDENCE`; the provenance reporter "always exits 0" and is "deliberately NOT a gate" — the gate is `scripts/data-lifecycle-report.cjs`. `platform-certificate.cjs` throws when a cross-checked artifact is missing.
- **Tests:** `tests/unit/platform/qualification-provenance.test.ts` (catalogued under `state-core`); `tests/acceptance/{platform-certificate,platform-soak-report,data-lifecycle-report,targeted-vs-full,architecture-discovery}.test.ts`; `tests/unit/platform/{platform-soak,architecture-ratchet,test-impact}.test.ts`; `tests/unit/test-layers.test.ts`; `tests/acceptance/state-migration-report.test.ts`.
- **Acceptance gates:** `test:platform-qualification`, `platform:certificate`, `soak:platform`, `verify:targeted`, `test:impact:verify`, `test:catalogue:check`, `architecture:ratchet`, `architecture:snapshot`, `acceptance:prestart`, `acceptance:attest`, `test:real-host-scale`. **`ci.yml` never runs the qualification tier** — it runs only `architecture:snapshot`, `pnpm test`, `test:postbuild`, `test:slow`.
- **Can be disabled?:** Yes — the public lane is already reduced to a diagnostic; `vitest.qualification.config.mjs` runs only when explicitly invoked, and real qualification is delegated to the private control repository.
- **Can be removed?:** No — `tests/unit/test-layers.test.ts` asserts across every workflow that no workflow in this repository runs the qualification tier, and asserts the producer scripts exist and are invoked; `config/architecture-baseline.json` is required by `architecture:ratchet`.
- **Can be replaced?:** The public hosted lane is explicitly replaceable by the private control plane; the certificate cannot stand in for a Root/Owner gate (`platform-certificate.cjs` declares `bypassesRootOrOwnerGate: false`).
- **Direct lateral dependencies?:** yes — `vitest.qualification.config.mjs → vitest.tiers.mjs`; qualification suites → their `producer` scripts; `platform-certificate.cjs` → compiled `dist-electron/electron/platform/{capability-registry,platform-health,architecture-ratchet,repo-scan}.js` (i.e. capability `runtime`); `qualification-corpus-provenance.cjs` → `trust-policy/trust-epoch.json`.
- **Shared mutable state?:** `trust-policy/trust-epoch.json` (single writer, only via `acceptance-evolution-bless.cjs --advance` in the protected workflow); `artifacts/platform-foundation/**` phase artifacts (generator-owned, cross-checked not transcribed); `artifacts/latest-package.json`/`latest-smoke.json`.
- **Root-trust relevance?:** **Central.** `qualification-corpus-provenance.cjs:152–158` reads exactly `trust-policy/trust-epoch.json` and reports `record.trust_epoch` (a fixture with no `trust-policy/` yields `null`, tested at `qualification-provenance.test.ts:122`). Provenance asserted: `schemaVersion 2`; `runner` keys exactly `["class","name","os"]` where class = `RUNNER_ENVIRONMENT ?? (CI ? "github-actions" : "local")`, name = `RUNNER_NAME ?? os.hostname()`, os = `os.type() os.release() os.arch()`; `commit = GITHUB_SHA ?? null`; `trustEpoch`; `corpus.{totalFiles,totalBytes,totalSkipped,commitmentDigest,commitmentAlgorithm:"sha256 over the sorted 'root\\0path\\0size' manifest"}`; `--redacted` omits `manifest` entirely and must leak no path/name/content; `--digest` must be independent of runner metadata. v1's `RUNNER_LABELS` field is removed. The certified surface includes `tests/acceptance/**`, `.github/workflows/ci.yml`, `scripts/acceptance-prestart.cjs` and `src/shared/bootstrap-audit.ts`. **`trust-epoch-finalization.yml` is the only place an epoch advances** — `workflow_dispatch` only, `refs/heads/main` only, `environment: boss-root-trust-owner`, refusing with `TRUST_EPOCH_FINALIZATION_REQUIRES_MAIN`; the committed epoch is `trust_epoch 24` / `boss-root-trust-24`, surface hash `6eaf71e9…`, epoch hash `33beb302…`. `trust-policy/requirement-retirements.json` is `[]`. `docs/root-trust-change-proposal-ci-qualification-pointer.md` records proposal 001 as AUTHORIZED and FINALIZED at commit `9d698c05…` as epoch 23 / surface `0ddb9000…`. `docs/acceptance/` holds one file; `Update-Plan/.../Phase-08-*.md` is self-labelled "RECONSTRUCTED, NOT AN ORIGINAL".

## 27. Installer / Bootstrap

> **NOT a declared capability.** `installer/**` is **not listed in `config/capability-modules.json` at all** and appears in no file under `config/capabilities/`. The bootstrap/composition-root paths **are** declared under module **`runtime`**: `electron/bootstrap/boot-module.ts`, `electron/bootstrap/runtime.ts`, `electron/main.ts`, `electron/preload.ts`, `electron/runtime-paths.ts`, `src/shared/bootstrap-audit.ts`. The other `electron/bootstrap/*.ts` files belong to their own capabilities.

- **Owner module:** **`runtime`** owns the bootstrap/composition-root paths (`electron/bootstrap/boot-module.ts`, `runtime.ts`, `main.ts`, `preload.ts`, `runtime-paths.ts`, `src/shared/bootstrap-audit.ts`); `installer/**` is **undeclared**. There is no capability literally named "Installer / Bootstrap".
- **Current implementation location:** `installer/windows/Installer.cs` (427 lines) + `installer/windows/installer.manifest`; `scripts/{package-installer,package-portable,embed-sandbox-launcher}.cjs`, `scripts/smoke-portable.ps1`, `scripts/start-codex-boss.ps1`; `Start-Codex-Boss.cmd`; `electron/bootstrap/` (26 entries incl. `shared/`); `electron/main.ts` (1460 lines), `electron/preload.ts`, `electron/runtime-paths.ts`, `src/shared/bootstrap-audit.ts`.
- **Inputs:** the payload ZIP appended to `Setup.exe`; `--dir`, `--user-data-root`, `--silent`, `--launch`; `app.getPath("userData")`; `safeStorage`; launcher args `-SmokeTest`/`-WaitForApp`.
- **Outputs:** unpacked install tree + `Codex Boss.exe`, `Uninstall.exe`, Start Menu/Desktop `.lnk`, HKCU Uninstall key (`Installer.cs:36,104–111`); `artifacts/latest-package.json`, `artifacts/latest-smoke.json`; `.codex-boss/launcher.log|electron.stdout.log|electron.stderr.log`.
- **State ownership:** `runtime` owns **no** durable namespace; the installer explicitly owns no user data — *"User data lives at `%LOCALAPPDATA%\Codex-Boss` and is NEVER touched here"* (`Installer.cs:14`).
- **Permissions:** `asInvoker`, `uiAccess="false"` (`installer.manifest:11–17`) — a per-user install that must never be mistaken for an elevation-requiring one. CI jobs use `permissions: contents: read`.
- **Dependencies:** `package-installer.cjs` consumes `package:portable` output and needs `C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`; `package-portable.cjs` requires `dist/index.html` + `dist-electron/electron/main.js` or throws "Run pnpm build before packaging".
- **Consumers:** `electron/main.ts` composes every `create*Module` from `electron/bootstrap/`; `scripts/start-codex-boss.ps1` launches `release\Codex Boss.exe` or `node_modules\electron\dist\electron.exe`.
- **Failure domain:** the launcher throws and shows a MessageBox on a missing build (`start-codex-boss.ps1:57–59`); a smoke test that times out (>15 s) is force-killed and fails; each boot module reports `READY`/`DEGRADED` via `reportBootHealth`.
- **Tests:** `tests/unit/bootstrap-ipc-modules.test.ts` (1508 lines), `tests/unit/bootstrap-{runtime,persistence,providers,provider-pool,research,knowledge,automation}.test.ts`, `tests/unit/runtime-paths.test.ts`, `tests/unit/platform/{platform-health,capability-manifest,dependency-graph}.test.ts`, `tests/acceptance/architecture-discovery.test.ts`.
- **Acceptance gates:** `package:portable`, `package:installer`, `acceptance:prestart`, `acceptance:root-hardening`, `acceptance:adversarial`, `build`; `scripts/smoke-portable.ps1` is run directly by the `package` CI job.
- **Can be disabled?:** Not in production — `electron/main.ts` calls the modules unconditionally. Individual IPC modules could be omitted from the composition root, and the installer is a standalone artifact that simply need not be run.
- **Can be removed?:** No for `electron/{main,preload,runtime-paths}.ts` (declared `runtime`). **Yes for `installer/**`** — it is undeclared and the portable ZIP is an independent deliverable.
- **Can be replaced?:** Yes — `Installer.cs` "is NOT an application packager": it only unpacks the portable payload, creates shortcuts and registers uninstall, so any packager that emits the same tree can replace it, and removal is not blocked by any manifest.
- **Direct lateral dependencies?:** yes — `electron/main.ts` → every `electron/bootstrap/*.ts` module (`createPersistenceModule`, `createStateCoreModule`, `createThemeIpcModule`, `createKnowledgeModule`, `createRuntimeModule`, `createProvidersModule`, …); `electron/bootstrap/persistence.ts` → `electron/{history-repository,store,api-settings}.ts` and 16 store classes.
- **Shared mutable state?:** yes — the `PERSISTENT_DATA_ENTRIES` migration (`runtime-paths.ts:19`); the single shared `TaskLedger` handed to both `StateStore` and the commander; `workspaces.ensureShims(appPath)`; the `.cache` env redirection (`TEMP`/`TMP`/`npm_config_cache`/`ELECTRON_CACHE`) in `start-codex-boss.ps1:30–33`.
- **Root-trust relevance?:** **Partial.** `src/shared/bootstrap-audit.ts` **is** on the Root Trust Surface (`ROOT_TRUST_SURFACE_PATHS`, `autonomous-evolution-trust.ts:120` and `root-trust-surface.json:11`). `electron/main.ts`, `preload.ts`, `runtime-paths.ts` are on the VERIFICATION surface only. `installer/**` is on neither list.

---

# Capabilities discovered but not in the required list

These are real capabilities of the system that the required list does not name. The first 17 are **declared capability modules** (so they already have `config/capabilities/<id>.yaml` manifests); the last three are undeclared surfaces discovered during this survey.

| Capability | Owner | Location | Purpose (one line) | Root-trust? |
|---|---|---|---|---|
| `attachments` | `attachments` (feature, `attachment.store@1`) | `electron/input/{attachment-router,attachment-store,attachment-upload}.ts`, `electron/bootstrap/attachment-ipc.ts` (4 files) | Attachment routing/store/upload; depends on `tasks` (`input-object`), `providers`, `persistence`, `status`. Tests: `tests/unit/bootstrap-ipc-modules.test.ts`. Gate: `acceptance:workbook`. | no |
| `automation` | `automation` (feature, `automation.lifecycle@1`; owns `telemetry`, `recovery`, `circuit-breaker`) | `electron/provider-automation.ts`, `electron/bootstrap/automation.ts` (2 files) | Visible prepare/send plus concurrent observe/capture, recovery scheduler wiring, telemetry. Highest lateral out-degree after `runtime` (7). Tests: `tests/unit/bootstrap-automation.test.ts`, `web-recovery-r6.test.ts`. | no |
| `conversation` | `conversation` (feature, `conversation.registry@1`) | `electron/bootstrap/conversation-ipc.ts` (1 file) | Conversation IPC registry. Tests: `tests/unit/bootstrap-ipc-modules.test.ts`. | no |
| `dispatch` | `dispatch` (feature, `dispatch.task@1`) | `electron/bootstrap/dispatch-ipc.ts` (1 file) | Task dispatch IPC surface; depends on `tenx` (`main-commander`, `workbook-*`), `tasks`. Tests: `tests/unit/bootstrap-ipc-modules.test.ts`. | no |
| `experience` | `experience` (feature, `experience.record@1`) | `electron/experience/` (2), `src/shared/experience.ts` (3 files) | Experience recorder/store; writes `.boss/experience.json` (declared namespace `experience`, owned by `persistence`). Tests: `tests/unit/experience-capability.test.ts`. | no |
| `host-status` | `host-status` (feature, `host.status@1`) | `electron/host/` (10), `electron/bootstrap/host-status-ipc.ts` (11 files) | Doctor, fault lab, soak harness, acceptance hub, host observer; the **only production wiring** for Fleet/Ten× registries. Tests: `tests/unit/host-*.test.ts`. | yes (`electron/engineering/*` partners; hub reads acceptance artifacts) |
| `identity` | `identity` (feature, `identity.session@1`) | `electron/identity/session-lifecycle-ledger.ts` (1 file) | Session lifecycle ledger; `session-lifecycle` namespace owned by `persistence`. Tests: `tests/unit/session-lifecycle.test.ts`. | no |
| `learning` | `learning` (feature, `learning.episode@1`) | `electron/learning/` (23 files) | Declared module behind Adaptive Learning (§20); declares `state: []` while writing 9 undeclared files under `.boss/learning`. Tests: `tests/unit/engine-phase-*.test.ts`. | no |
| `node` | `node` (feature, `node.capability@1`) | `electron/node/{node-capability-registry,node-inspector}.ts` (2 files) | Node capability registry + device inspector; the `node-registry` namespace is owned by `persistence`. Wired at `persistence.ts:143`, `main.ts:703,1039`. Tests: `tests/unit/node-capabilities.test.ts`, `standalone-node.test.ts`. | no |
| `project` | `project` (feature, `project.state@1`) | `electron/project/{project-state,task-completion-recorder}.ts` (2 files) | Project state + task completion; `project-state` namespace owned by `persistence`. Tests: `tests/unit/durable-state-ownership.test.ts`. | no |
| `remote` | `remote` (feature, `remote.relay@1`) | `electron/remote-relay.ts` (1 file) | Opt-in WeChat/QQ listener lifecycle + validated record parser. Tests: `tests/unit/remote-capability.test.ts`. | no |
| `settings` | `settings` (feature, `settings.ipc@1`, `settings.panes@1`) | `electron/bootstrap/settings-ipc.ts` (1 file) | Settings IPC surface. Tests: `tests/unit/bootstrap-ipc-modules.test.ts`. | no |
| `status` | `status` (feature, `status.snapshot@1`) | `electron/bootstrap/status-ipc.ts`, `electron/cache/`, `electron/evaluation/`, `electron/evidence-engine.ts`, `electron/repro-snapshot.ts`, `electron/self-{case-record,cognition,diagnosis}/`, `electron/telemetry/` + 21 `src/shared` files (40 files) | The owner module of **Evidence, Self Cognition, Self Diagnosis and Self Case Record**; the largest single owner of the "self-*" surface. Tests: `tests/unit/self-*/`, `tests/unit/host-*.test.ts`. | **yes** |
| `task-creation` | `task-creation` (feature, `task.creation@1`) | `electron/bootstrap/task-creation-ipc.ts` (1 file) | Task creation IPC. Tests: `tests/unit/bootstrap-ipc-modules.test.ts`. | no |
| `theme` | `theme` (feature, `theme.ipc@1`; **requires `theme.registry@1`**) | `electron/theme/` (5), `electron/bootstrap/theme-ipc.ts`, 6 `src/shared` files (12 files) | Theme service/storage/knowledge + UI-surface registry. Owns the only declared **optional-vs-required** cross-capability edge into `knowledge`. Tests: `tests/unit/theme-*.test.ts`, `tests/acceptance/theme-engine.test.ts`. Gate: `acceptance:theme`. | no |
| `workspace` | `workspace` (feature, `workspace.selection@1`) | `electron/workspace/` (10), `electron/bootstrap/workspace-ipc.ts`, `electron/input/repo-manifest.ts`, 8 `src/shared` files (20 files) | Workspace registry/selection/paths, artifact backbone, external-session ledger; `workspaces` + `workspace-selection` namespaces owned by `persistence`. Gate: `acceptance:workspace-paths`. | no |
| **Capability broker / permission surface** (undeclared) | `runtime` (kernel) | `electron/capability/{permission-contract,authorization,capability-broker,credential-reference,plugin-contract,plugin-host,theme-capability}.ts`, `plugin-runner.cjs`, `integration/{execution-authorization,boundary-inventory}.ts`, `plugins/` | Default-deny authorization engine + broker + plugin sandbox + credential references + the high-privilege **boundary inventory**. **No production consumer was found**: imports appear only in `tests/acceptance/capability-escape.test.ts` and `tests/unit/capability/*.test.ts`; `electron/main.ts` never constructs a broker. Tests are catalogued under `security`. Gates: `capability:surface`, `acceptance:root-hardening`. | yes (records the `legacy` route for `root-authority`, `protected-surface-guard`, `credential-boundary`; asserts those files exist) |
| **Platform architecture registry & ratchet** (undeclared) | `runtime` (kernel) | `electron/platform/{capability-contract,capability-manifest,capability-registry,dependency-graph,state-ownership,architecture-ratchet,platform-health,repo-scan,test-impact,coordination-store,coordination-recorder,external-compatibility}.ts` | Read-only, **explicitly not a service locator**: loads the 27 manifests, builds the graph and the ownership registry, and enforces 6 ratchets (absolute: zero required cycles, zero kernel→feature imports, zero literal IPC in `main.ts`; monotone: boot-module count, edge count, namespace count, baselined in `config/architecture-baseline.json`). Tests: `tests/unit/platform/*.test.ts` (many `alwaysRun`). Gates: `architecture:{graph,impact,ownership,ratchet,snapshot}`, `acceptance:architecture`. | **yes** (the ratchet is the enforcement point for required-check and state-ownership declarations) |
| **Ten× coordination surface** (undeclared name; module IS `tenx`) | `tenx` (feature, `tenx.artifact@1`, `tenx.network@1`) | `electron/tenx/{artifact-ledger,dynamic-scheduler,host-adapter,knowledge-conflicts,knowledge-pipeline,knowledge-space,knowledge-sync,login-health,observability,platform-audit,provider-matrix,device-inspector}.ts`; `src/shared/tenx/{index,contracts,artifact,inspection,knowledge,scheduler,fleet,network,node,session}.ts`; `src/shared/{coordination-economics,coordination-ledger,optional-review}.ts`; `electron/platform/{coordination-store,coordination-recorder}.ts` | Distributed artifact ledger, dynamic scheduling, knowledge sync/conflict, provider matrix, login health, fleet aggregate, platform-neutrality audit, and the Gate-8 coordination economics. `src/shared/tenx` is provably platform-neutral (`tests/unit/tenx-phase-10q.test.ts`). Gates: `economics:{collect,evaluate,verify,fixtures}`, `verify:certificate`. | no (`coordination-economics.ts` carries a pipeline-composition qualification rule: verification is an invariant and may never be the A/B variable) |

Also notable: **`src/shared/coordination-store.ts` is declared under `tenx` in `capability-modules.json` but does not exist**; only `electron/platform/coordination-store.ts` exists (see *Gaps*).

---

# Gaps and uncertainties

## Defects found in the declared ownership map (`config/capability-modules.json`)

1. **Dangling path.** `tenx` lists `src/shared/coordination-store.ts`, which **does not exist**. Only `electron/platform/coordination-store.ts` exists. The `extend-capability-modules.cjs` validator only checks the paths in its own `EXTRA` table, not the base list, so this never fails a check.
2. **One file claimed twice.** `electron/platform/coordination-store.ts` appears **twice** in the `runtime` list (a literal duplicate, not a cross-capability conflict).
3. **Owned *and* exempt.** `src/shared/compatibility.ts` is listed under `persistence` (line 85) **and** in the `exempt` map (line 333), whose stated reason is that owning it there would let two capabilities claim the same file. The extend script does not report this because the second claimant is the same capability, so the "would be owned by both" branch never fires. This is the only exempt/owned collision; `src/renderer` has zero owned files.
4. **`persistence.yaml` disagrees with the map.** The manifest's own `modules` lists only `electron/bootstrap/persistence.ts`, while `capability-modules.json` gives `persistence` 13 paths. `config/architecture.cjs#collectImports` reads `modules`, so the declared 3-edge graph is computed over 25 boot modules, not the 594-file surface. The real lateral graph has **187 edge-kinds**.

## Undeclared durable state

- **32 namespaces are declared, one owner each, zero conflicts** — but a large amount of durable state is written outside any declared namespace, so the one-authoritative-writer rule does not cover it. Confirmed undeclared writers include: `electron/learning/**` (9 files under `.boss/learning`), `electron/runtime-intelligence/**` (`<dataRoot>/.boss/runtime-intelligence`), `electron/self-case-record/case-store.ts` (`case-record.jsonl`), `electron/self-evolution/**` (`governanceRoot`: `evolution-control.json`, `evolution-frozen.sentinel`, `evidence.jsonl`, `root-audit-ledger.jsonl`, `runs/*.json`, `stable-pointer.json`), `electron/tenx/**` (`boss/tenx/nodes.json`, `boss/tenx/fleet.json`), `electron/engineering/**` (`engineering-loop.json`, `engineering-recovery.json`).
- `capability-modules.json` names 159 distinct `*.json`/`*.jsonl` file basenames across the source tree, versus 32 declared namespaces.
- Two capabilities write the same class of file from different roots: `electron/knowledge/knowledge-base.ts` (`knowledge`) and `electron/engineering/improvement-loop.ts:234` (`engineering`, root `artifacts/acceptance/knowledge-base.json`).
- `research` writes **outside its declared namespaces into the user workspace**: `<workspace>/experiments/bench-*.js` and `<workspace>/Research/**`.

## Unwired / orphan surfaces

- `electron/capability/**` (the whole capability broker, authorization engine, plugin host, credential references, boundary inventory) has **no production consumer**; `electron/main.ts` never constructs a broker, so `ExecutionGate`'s authorization branch is dead in the shipped wiring (`new ExecutionGate()` at `main.ts:910`).
- `electron/self-engineering/{diagnosis,correction-store,self-mod-sandbox}.ts`, `src/shared/self-diagnosis.ts`, `electron/computer/perception-loop.ts`, `electron/computer/provider-page-repair.ts`, `electron/tenx/host-adapter.ts` (a **non-exported, unimplemented** "adapter seam"), `src/shared/autonomy-supervisor.ts` and `electron/commander/token-budget-manager.ts` have **no non-test importer**.
- `src/shared/self-cognition`, `src/shared/self-diagnosis`, `src/shared/self-case-record` and their hosts have **no production consumer at all**: they run only from CLI scripts (`scripts/self-view.cjs`, `scripts/self-diagnosis.cjs`, `scripts/self-case-record.cjs`) and unit tests. None of those three scripts is a `package.json` entry, so none is a gate.
- Several scripts exist that no `package.json` script invokes: `scripts/host-acceptance.cjs`, `scripts/host-evidence.cjs`, `scripts/acceptance-research-audit.cjs`, `scripts/acceptance-research-offline.cjs`, `scripts/runtime-intelligence-report.cjs`, `scripts/runtime-intelligence-diff-guard.cjs`, `scripts/self-*.cjs`.

## Flag and disable-path uncertainties

- `src/shared/host-maturity-flags.ts` defines `HOST_FLAG_IDS` and `resolveHostFlags`, but **no consumer of `resolveHostFlags`/`hostCapabilityEnabled` exists in `electron/` or `scripts/`** — the "Host-M capabilities default OFF" claim is not enforced anywhere observable.
- `src/shared/adaptive-flags.ts` **does** have real enforcement (all five flags default `false`; `adaptiveCapabilityEnabled` requires umbrella + own flag), and `EvolutionKillSwitch` **does** have a real, uncached, Owner-only freeze. For everything else, "Can be disabled?" is `UNKNOWN` — there is no general kill-switch or capability-toggle mechanism in the composition root.

## Field-level UNKNOWNs / NOT_FOUNDs

- `Can be disabled?` = UNKNOWN (no flag or switch found in source): Commander, Scheduler, Runtime Registry, Role Router, Task State/Task Ledger (no — enforced by construction), Research, Knowledge, Fleet, GitHub (config flag exists, `enabled:false`), Runtime Intelligence, Self Cognition / Self Diagnosis / Self Case Record (nothing to disable — not wired), Engineering, Installer/Bootstrap.
- `Evidence`: no capability-specific `acceptance:*` gate for the status-side evidence engine; no test imports `buildEvidenceBundle`; nothing covers `electron/research/evidence/**`; `hostEvidenceInspector`/`hostAcceptanceHub` flags are unenforced.
- `Self Diagnosis`: the engineering half (`src/shared/self-diagnosis.ts`, `electron/self-engineering/diagnosis.ts`) is dead code.
- `Self Cognition`: `NOT_FOUND` for a capability-specific acceptance script.
- `Qualification`: the interface between "required-check declaration" and this surface is `src/shared/promotion-checks.ts:35` (`REQUIRED_PROMOTION_CHECKS = ["quality","unit","acceptance","package"]`) — a `promotion`/security surface, not a qualification one; whether `src/shared/coordination-economics.ts`'s `MANDATORY_GATE_STAGES` counts as a required-check declaration is UNKNOWN.
- `Adaptive Learning`: whether the adaptive reranker is *intended* to be wired (as opposed to deliberately unwired) is UNKNOWN; the only evidence is that `RoleRouter` is constructed without one.
- Namespace-as-path mismatches: `research-runs`, `theme-packages` and `theme-registry` are declared as namespaces but appear as no literal path string in the source; the concrete files they correspond to could not be established from the source alone.
- `Provision`/`permission` declarations are empty for **all 27 manifests** (`permissions: []` and `surface: []` everywhere). The only permission vocabulary in the repository is `src/shared/permission.ts` (`filesystem|repo|network|secret|side-effect`, `SecurityClass PUBLIC|INTERNAL|SECRET|GUARDIAN`) plus `electron/capability/permission-contract.ts`, which has no production consumer. A permission inventory per capability is therefore **not derivable from the manifests** — every "Permissions" field above was derived from code, not declaration.
- Coupling between the graphical requirement "one detailed section per capability" and reality: 15 of the 27 required names are not declared capabilities. Those sections name the declared owner module explicitly rather than inventing a manifest.
